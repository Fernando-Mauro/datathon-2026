---
phase: 03.1
slug: hey-banco-design-system-variante-a-conversational-havi
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-25
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: 03.1-RESEARCH.md §Validation Architecture (lines 1446–~1510).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | **None installed** — Phase 1-3 honored "no test runner" decision; Phase 03.1 continues. |
| **Config file** | none |
| **Quick run command** | `bun run lint && bun run typecheck` |
| **Full suite command** | `bun run lint && bun run typecheck && bun run audit && bun run build` (Phase 1 gauntlet) |
| **Estimated runtime** | ~10s quick / ~60-120s full (build + new fonts + framer-motion bundle) |

---

## Sampling Rate

- **After every task commit:** `bun run lint && bun run typecheck`
- **After every plan completes (wave merge):** `bun run lint && bun run typecheck && bun run audit && bun run build`
- **Before `/gsd-verify-work`:** Full suite green + 10 manual flow walkthroughs (M-1 .. M-10)
- **Max feedback latency:** ~120s (build dominates; first compile after font/Tailwind changes is slower)

---

## Per-Task Verification Map

*Filled by gsd-planner. Each task in PLAN.md must have `<automated>` verify or be explicit checkpoint:human-verify with rationale.*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | (regression) AUTH-03/04/05 + new visual deliverables | TBD | TBD | TBD | TBD | TBD | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] **No test runner install** — explicit decision, Layers 2-3 intentionally empty
- [ ] **`bun add motion@^12.38.0`** — MUST happen in the first plan that imports `motion/react`. Fail-fast if missing.
- [ ] **No bundle analyzer** — defer to Phase 4 (Hosting)
- [ ] **No visual regression tooling** — manual M-6/M-7 covers

*Phase 03.1 ships with manual + static validation only — mirrors Phase 2-3 D-29 / Wave 3 manual approach.*

---

## Manual-Only Verifications

| ID | Behavior | Coverage | Why Manual | Test Instructions |
|----|----------|----------|------------|-------------------|
| M-1 | Sign-up + email verification still works | regression AUTH-03 | Real Cognito sandbox flow | Incognito → `/login` → "Create account" → register with `+test{N}@gmail.com` alias → enter code → expect redirect to `/app` chat home |
| M-2 | Sign-in + sign-out works | regression AUTH-03 | Real Cognito flow | Incognito → `/login` → enter existing test user creds → expect `/app` chat home → click sign-out → expect `/` |
| M-3 | Reset password flow works | regression AUTH-03 | Real email round-trip | `/login` → "Forgot password?" → enter email → check inbox for code → enter new password → sign in with new password |
| M-4 | `<AuthGuard>` still redirects on sub-routes | regression AUTH-04 | Real route navigation | Incognito → paste `http://localhost:3000/app/grafica/comida` → expect redirect to `/login?from=%2Fapp%2Fgrafica%2Fcomida` |
| M-5 | Session persists across refresh | regression AUTH-05 | Token storage | Sign in → refresh page → still logged in (no redirect to /login) |
| M-6 | Visual replatform — Geist gone, IBM Plex everywhere | new (D-45/D-52) | Visual diff | Open `/`, `/login`, `/app` — all render in IBM Plex Sans body + Plex Serif headings/numerals; dark hey palette; no Geist visible. `grep -ri "Geist" app/` returns 0 hits. |
| M-7 | Custom Authenticator theme on `/login` | new (D-44) | Visual diff | `/login` form: black bg (`#000`), white text, blue (`#3478F6`) primary button, pill border-radius on inputs/buttons, IBM Plex font. Sign-in still works. |
| M-8 | Conversational chat home — greeting + snapshot + composer | new (D-50, D-49) | Real interaction | Sign in → land on `/app` → see "hey, {firstName}" greeting (from Cognito name attribute), balance card + sparkline, composer fixed bottom. Type "transferir" → typing dots animation → transfer action card appears |
| M-9 | Sub-route navigation with framer-motion slide | new (D-46, D-51) | Real interaction | From chat home, tap "Ver gráfica" CTA → `/app/grafica/general` slides in from right (smooth ease-out) → tap back chevron → returns to `/app` chat with messages preserved |
| M-10 | localStorage persistence + sign-out clear | new (D-47) | Real persistence | Send a message in chat → refresh page → message visible (restored from `havi-chat-v1`); sign out via SignOutButton → sign in again → chat resets to intro greeting (Hub.listen cleared the key) |

---

## Validation Sign-Off

- [ ] All implementation tasks have `<automated>` verify (lint+typecheck or build) or are explicit `checkpoint:human-verify` with rationale
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (lint+typecheck after each commit)
- [ ] Wave 0 captures `bun add motion@^12.38.0` as gating dependency
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter (after planner + verifier pass)
- [ ] All 10 manual flows (M-1..M-10) walked through and approved before phase complete

**Approval:** pending — auditor + verifier sign off after execution
