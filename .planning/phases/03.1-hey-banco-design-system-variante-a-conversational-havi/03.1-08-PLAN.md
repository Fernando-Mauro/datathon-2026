---
phase: 03.1
plan: 08
type: execute
wave: 7
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified:
  - app/page.tsx
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: []
phase_goal_alignment: "Phase 03.1 close-out — public home `/` restyle (D-43 wordmark + dark palette), Phase 1 verification gauntlet, ten manual flow walkthroughs (M-1..M-10) covering AUTH-03/04/05 regression + new visual/flow deliverables, then close-out (SUMMARY + STATE + ROADMAP + REQUIREMENTS update). The phase ships green."
tags:
  - close-out
  - public-home
  - manual-verification
  - phase-summary
user_setup: []

must_haves:
  truths:
    - "/ renders bg-hey-bg + IBM Plex + <Wordmark size=\"lg\"> instead of next.svg + scaffold copy"
    - "/ retains the Sign in link → /login (Phase 2 surface)"
    - "Phase 1 gauntlet final pass: lint && typecheck && audit && build all exit 0"
    - "All ten manual flows M-1..M-10 walked through and approved by user (autonomous: false — requires human verification)"
    - "M-1 Sign-up + email verification regression PASS"
    - "M-2 Sign-in + sign-out regression PASS"
    - "M-3 Reset password regression PASS"
    - "M-4 AuthGuard sub-route redirect PASS"
    - "M-5 Session persistence across refresh PASS"
    - "M-6 Visual replatform — no Geist anywhere PASS"
    - "M-7 Custom Authenticator theme PASS"
    - "M-8 Conversational chat home flow PASS"
    - "M-9 Sub-route slide nav PASS"
    - "M-10 localStorage chat persistence + sign-out clear PASS"
    - "Phase 03.1-08-SUMMARY.md exists capturing the full close-out"
    - "STATE.md updated: progress, completed_phases, decisions log entry for 03.1"
    - "ROADMAP.md Phase 03.1 entry marked [x] with goal text + plan list filled in"
    - "REQUIREMENTS.md Validated section gains note: visual replatform shipped 2026-04-25 (no new requirement IDs since UX phase)"
  artifacts:
    - path: "app/page.tsx"
      provides: "Public home with hey palette + wordmark + preserved sign-in link"
      contains: "Wordmark"
    - path: ".planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-08-SUMMARY.md"
      provides: "Plan 08 close-out: public home delta + 10 manual flow walkthrough results + final gauntlet evidence"
    - path: ".planning/STATE.md"
      provides: "Updated state — Phase 03.1 complete, plan count, decisions log"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 03.1 entry filled in with goal + plans + status [x]"
  key_links:
    - from: "/"
      to: "/login"
      via: "Link href=/login (preserved + restyled)"
      pattern: "/login"
    - from: "Plan 08 close-out"
      to: "Phase 03.1 ship state"
      via: "STATE.md updated + ROADMAP.md filled + SUMMARY committed"
      pattern: "Phase 03.1.*complete"
---

<objective>
Phase 03.1 Plan 08 — Last plan. Three responsibilities:

1. **Public home restyle** — Modify `app/page.tsx` to apply the hey palette + `<Wordmark size="lg">` + IBM Plex (the body class set in Plan 01 already provides Plex globally; this is just the page-local hero swap). Drop the Next.js scaffold logo + scaffold copy. Preserve the `/login` link (Phase 2 surface). Restyle minimally — UI-SPEC §"Public home `/`" (line 695) says "minimum: keep current scaffold copy + apply tokens". RESEARCH OQ-2 recommends ~30min restyle.

2. **Manual gauntlet (M-1..M-10)** — Ten manual walkthroughs from VALIDATION.md. The plan is autonomous: false because manual verification is non-automatable (real Cognito flows + visual diffs + interactive UX). The executor walks the user through each flow with clear instructions, expected outcomes, and a "PASS / FAIL — describe issue" prompt. The final gauntlet `lint && typecheck && audit && build` must exit 0 BEFORE starting M-1..M-10.

3. **Phase close-out** — After the user signs off on M-1..M-10, write SUMMARY.md + update STATE.md + ROADMAP.md + REQUIREMENTS.md note. Commit the final state.

Purpose: Ship Phase 03.1 green. After this plan, every Phase 2 D-29 flow + Phase 3 D-30/D-31 invariant is re-verified; every new D-42..D-52 deliverable is observed live; the project STATE reads `Phase 03.1 complete` so Plan 4 (Hosting + CI) can begin.

Output: One file rewrite, ten manual walkthrough check-ins, three planning-doc updates. autonomous: false because of M-1..M-10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-CONTEXT.md
@.planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-RESEARCH.md
@.planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-UI-SPEC.md
@.planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-VALIDATION.md
@.planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-PATTERNS.md
@.planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-01-PLAN.md
@.planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-02-PLAN.md
@.planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-03-PLAN.md
@.planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-04-PLAN.md
@.planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-05-PLAN.md
@.planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-06-PLAN.md
@.planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-07-PLAN.md
@CLAUDE.md
@AGENTS.md

<interfaces>
Current public home `/` is the Phase 1 scaffold — `app/page.tsx` imports next/image + next/link, renders Next.js logo + scaffold copy + Vercel/Docs links + a `/login` link in the top-right. PRESERVE the `/login` Link (Phase 2 entry point). DROP the next.svg/vercel.svg images, the scaffold copy, the dark:invert/dark:bg-black modifiers (D-42 — dark-only).

Manual gauntlet from VALIDATION.md M-1..M-10:

| ID   | Behavior |
|------|----------|
| M-1  | Sign-up + email verification (regression AUTH-03) — incognito → /login → "Create account" → register +test{N}@gmail.com → enter code → expect /app chat home |
| M-2  | Sign-in + sign-out (regression AUTH-03) — existing test creds → /app chat home → click sign-out → expect / |
| M-3  | Reset password (regression AUTH-03) — /login → "Forgot password?" → enter email → check inbox → enter new password → sign in |
| M-4  | AuthGuard sub-route redirect (regression AUTH-04) — incognito → http://localhost:3000/app/grafica/comida → expect redirect to /login?from=%2Fapp%2Fgrafica%2Fcomida |
| M-5  | Session persistence (regression AUTH-05) — sign in → refresh page → still logged in |
| M-6  | Geist gone, IBM Plex everywhere — open /, /login, /app — all in IBM Plex; grep -ri "Geist" app/ returns 0 hits |
| M-7  | Custom Authenticator theme on /login — black bg, white text, blue primary button, pill borders, IBM Plex; sign-in still works |
| M-8  | Conversational chat home — sign in → /app → see "hey, {firstName}" + balance + sparkline + composer; type "transferir" → typing dots → transfer card |
| M-9  | Sub-route slide nav — chat home → "Ver gráfica" CTA → /app/grafica/general slides in from right → tap back chevron → return to /app with messages preserved |
| M-10 | localStorage persistence + sign-out clear — send message → refresh → message visible (restored from havi-chat-v1); sign out → sign in again → chat resets to intro greeting |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restyle app/page.tsx (public home) with hey palette + Wordmark + preserved /login link; final pre-gauntlet build</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (current scaffold — full file)
    - app/_components/Wordmark.tsx (Plan 02 output — Wordmark component)
    - .planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-CONTEXT.md D-43 — wordmark in headers of /, /login, /app
    - .planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-UI-SPEC.md §"Copywriting Contract / Public home /" (line 695)
    - .planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-RESEARCH.md §"Open Questions / 2. Should Phase 03.1 also restyle the public home / page?" (lines 1403-1406) — recommend minimal restyle
    - .planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-PATTERNS.md §"app/page.tsx (MODIFY)" (line 80)
  </read_first>
  <action>
    Restyle `app/page.tsx`. Per UI-SPEC line 695 + RESEARCH OQ-2: minimum restyle — apply tokens, swap logo for wordmark, drop scaffold-template content. Keep the `/login` link (Phase 2 entry surface).

    Replace the file entirely with this shape:

    - Server component (no `"use client"`).
    - Two imports only: `Link from "next/link"`, `Wordmark from "@/app/_components/Wordmark"`.
    - Default-export `Home()` returning a wrapper `<div className="flex flex-col flex-1 min-h-screen items-center justify-center bg-hey-bg text-hey-fg-1 px-8">`.
    - Top-right corner: `<Link href="/login" className="absolute top-4 right-4 rounded-full border border-hey-divider bg-hey-surface-2 px-4 py-2 text-sm text-hey-fg-1 hover:bg-hey-surface-3 transition-colors">Iniciar sesión</Link>`.
    - Centered hero `<main className="flex flex-col items-center text-center gap-6 max-w-md">` containing:
      - `<Wordmark size="lg" as="h1" />`
      - `<p className="text-[15px] text-hey-fg-2 leading-[1.5]">Demo conversacional de banca con HAVI — hey, banco design system.</p>`
      - `<Link href="/login" className="rounded-full bg-hey-blue text-white px-6 py-3 text-sm font-medium hover:bg-hey-blue-hover transition-colors">Comenzar</Link>`

    Notes:
    - Drop `import Image from "next/image"` — no `next.svg` or `vercel.svg` references.
    - Body classes already provide `bg-hey-bg text-hey-fg-1 font-sans` from Plan 01. Adding them on the wrapper `<div>` is redundant but explicit.
    - Top-right "Iniciar sesión" link uses the same pill-rounded surface as `<SignOutButton>` for visual consistency.
    - The description copy is intentionally generic; UI-SPEC line 695 doesn't lock the wording, only requires es-MX.
    - Drop `dark:invert`, `dark:bg-black` modifiers — Phase 03.1 is dark-only (D-42 + RESEARCH Pitfall 9). The design has no light variant.

    After the rewrite, run the FINAL Phase 1 verification gauntlet:

        bun run lint
        bun run typecheck
        bun run audit
        bun run build

    All four MUST exit 0. If any fails, halt — DO NOT proceed to Task 2 (manual gauntlet). The build is the load-bearing check: validates the entire phase compiles together (Plans 01-07 outputs + Plan 08 public home).
  </action>
  <verify>
    <automated>cd /home/fernando/Documents/datathon-2026 && grep -q '<Wordmark' app/page.tsx && grep -q 'size="lg"' app/page.tsx && grep -q '"@/app/_components/Wordmark"' app/page.tsx && grep -q '"/login"' app/page.tsx && grep -q 'Iniciar sesión' app/page.tsx && grep -q 'bg-hey-bg' app/page.tsx && ! grep -q "next.svg" app/page.tsx && ! grep -q "vercel.svg" app/page.tsx && ! grep -q "dark:invert" app/page.tsx && ! grep -q "dark:bg-black" app/page.tsx && ! grep -q "Get started, edit" app/page.tsx && ! grep -rq "Geist" app/ && bun run lint && bun run typecheck && bun run audit && bun run build</automated>
  </verify>
  <done>
    `app/page.tsx` rewritten: imports `<Wordmark>` from `@/app/_components/Wordmark`, renders `<Wordmark size="lg" as="h1">`, two `/login` links (top-right "Iniciar sesión" + center "Comenzar"), Spanish copy. Drops next.svg, vercel.svg, dark:invert, dark:bg-black, scaffold copy. NO Geist references anywhere in `app/`. Full Phase 1 gauntlet (`lint && typecheck && audit && build`) exits 0 — Phase 03.1 is BUILDABLE end-to-end.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual gauntlet — walk M-1..M-10 with the user</name>
  <what-built>
    Plans 01-07 + Task 1 of Plan 08 are shipped:
    - Tokens + fonts + motion installed (Plan 01)
    - Authenticator theme + login wrap + Wordmark (Plan 02)
    - Mock data + types + patterns + format helper (Plan 03)
    - Nine primitive components + persistence hook + MotionConfig template + curated icon SVGs (Plan 04)
    - Nine composition components (Sparkline, BarChart, SnapshotCard, ActionCard, AlertCard, TransferCard, TransactionRow, MessagesList, ChatMessage) (Plan 05)
    - Chat home REPLACE + SignOutButton restyle (Plan 06)
    - Four sub-routes: /app/grafica/[categoria], /app/movimientos, /app/comparativa, /app/agente (Plan 07)
    - Public home /  restyled with hey palette + Wordmark (Plan 08 Task 1)
    - Final Phase 1 gauntlet (lint + typecheck + audit + build) green

    Now: 10 manual flows verify Phase 2 D-29 + Phase 3 D-30/D-31 + Phase 03.1 D-42..D-52 LIVE.
  </what-built>
  <how-to-verify>
    The user runs `bun run dev` (or rebuilds + serves locally if preferred) and walks through ALL TEN flows. The executor pauses after each flow to capture PASS/FAIL + notes. ANY failure halts the phase — Plan 08 does not close out until all ten are PASS.

    **Pre-flight:**
    1. `bun run dev` — confirm dev server boots without errors at http://localhost:3000.
    2. `npx ampx sandbox` — confirm Cognito sandbox is up (us-east-1_6l4dSfRCz per STATE.md). If down, redeploy: `npx ampx sandbox --once`.
    3. Ensure at least one test user exists in Cognito (Phase 2 manual flow created `+test1@gmail.com` per Plan 02-05 SUMMARY). If unknown, create a fresh one in M-1.

    **M-1 — Sign-up + email verification (regression AUTH-03)** (~3 min)
    - Open new incognito window → http://localhost:3000/login.
    - Click "Create Account" tab.
    - Enter email `youremail+test{N}@gmail.com` (use a real Gmail address — `+test1`, `+test2`, etc. — Phase 2 alias pattern), Name "Demo User", strong password.
    - Submit. Authenticator should show verification-code input.
    - Open the email; copy the 6-digit code; paste into the form.
    - Submit. Expected: redirect to `/app` chat home with greeting "hey, Demo." + snapshot card + composer.
    - User reports: PASS or FAIL with description.

    **M-2 — Sign-in + sign-out (regression AUTH-03)** (~1 min)
    - In the same window (signed in from M-1), navigate to `/app/agente` either by typing "no entiendo" in the composer → fallback CTA "Hablar con agente" OR direct URL `http://localhost:3000/app/agente`.
    - In agente header, top-right shows "Cerrar sesión". Click it.
    - Expected: redirect to `/` (public home — Wordmark visible).
    - Click "Iniciar sesión" → /login → enter test creds → submit.
    - Expected: redirect to `/app` chat home.
    - User reports: PASS or FAIL.

    **M-3 — Reset password (regression AUTH-03)** (~3 min)
    - Sign out (per M-2 if still signed in) → /login.
    - Click "Forgot your password?" link.
    - Enter email of an existing test user.
    - Authenticator sends a reset code to email. Copy it; paste into next form along with a new password.
    - Submit. Expected: returned to sign-in screen.
    - Sign in with the new password. Expected: /app chat home.
    - User reports: PASS or FAIL. (If the sandbox SES isn't configured for reset emails, this flow may legitimately fail — record as "skipped per Phase 2 02-05 SUMMARY pattern" if so.)

    **M-4 — AuthGuard sub-route redirect (regression AUTH-04)** (~30s)
    - New incognito window (signed-out state).
    - Paste URL: `http://localhost:3000/app/grafica/comida`.
    - Expected: immediate redirect to `/login?from=%2Fapp%2Fgrafica%2Fcomida` (URL-encoded).
    - User reports: PASS or FAIL. (Check the URL bar — it MUST contain the `from=` param with the URL-encoded path.)

    **M-5 — Session persistence (regression AUTH-05)** (~30s)
    - Sign in (incognito or signed-in window from M-4 if still in `/login?from=...`).
    - On `/app` chat home, refresh the page (F5).
    - Expected: still on `/app`, no flash of `/login`, greeting + chat persist.
    - User reports: PASS or FAIL.

    **M-6 — Visual replatform: Geist gone, IBM Plex everywhere** (~2 min)
    - Open DevTools → Computed → check `font-family` on body for `/`, `/login`, `/app`.
    - All three must show `"IBM Plex Sans"` (or system fallbacks if Plex hasn't loaded yet — should load within ~500ms via next/font).
    - Headings (Wordmark, snapshot balance, alerts) should use IBM Plex Serif.
    - Run from terminal: `grep -ri "Geist" /home/fernando/Documents/datathon-2026/app /home/fernando/Documents/datathon-2026/package.json` — MUST return zero hits.
    - User reports: PASS or FAIL.

    **M-7 — Custom Authenticator theme on /login** (~1 min)
    - Sign out → /login.
    - Visual check:
      - Background: solid black `#000000`.
      - Text: white.
      - Primary submit button (`Sign in` or `Create Account`): blue `#3478F6` background, white text.
      - Input borders: pill-shaped (`border-radius: 9999px`).
      - Input/labels font: IBM Plex Sans.
    - Sign in. Expected: still works (theme is cosmetic only, AUTH-03 unchanged).
    - User reports: PASS or FAIL with screenshot if any element looks unstyled.

    **M-8 — Conversational chat home: greeting + snapshot + composer + typing→response** (~2 min)
    - Sign in → /app.
    - Verify:
      - Greeting: "hey, {firstName}." in IBM Plex Serif.
      - Snapshot card: SALDO TOTAL eyebrow + balance ($12,450.30 in Plex Serif) + GASTASTE ESTA SEMANA + spent + sparkline.
      - SuggestionChips row above composer with the four prompts.
      - Composer: textarea + circular blue send button (when text entered).
    - Type "transferir" in composer → press Enter.
    - Expected: user-text bubble appears (right-aligned, blue) → typing dots indicator (3 pulsing dots, left-aligned bubble) for 800-1500ms → transfer card appears (recipient "Mariana López", amount $500.00, "Continuar" button).
    - Type "gasté" → Enter.
    - Expected: typing dots → HAVI text bubble + spend-actions card (4 BigActionButtons in 2x2 grid).
    - User reports: PASS or FAIL.

    **M-9 — Sub-route slide nav (forward + back)** (~2 min)
    - Continue from M-8 chat home.
    - Click "Ver gráfica" (ActionPill in snapshot card) — should call `onNavigate("/app/grafica/general")`.
    - Expected: /app/grafica/general slides in from RIGHT (smooth ease-out, ~320ms). Header shows "Gasto por categoría" with back chevron + small HAVI ring on the right.
    - BarChart visible: ESTA SEMANA + total + segmented bar (motion grow animation) + breakdown rows.
    - Click the back chevron in the header.
    - Expected: returns to `/app` chat home; messages from M-8 still present (localStorage preserved).
    - Repeat with "Movimientos" pill → /app/movimientos slides in from right.
    - User reports: PASS or FAIL.

    **M-10 — localStorage persistence + sign-out clear** (~2 min)
    - On chat home, type "movimientos" → Enter (this should produce a screen-action card).
    - Refresh the browser (F5).
    - Expected: chat messages from before refresh STILL VISIBLE (intro + snapshot + user "movimientos" + screen-action card response).
    - DevTools → Application → Local Storage → http://localhost:3000 — confirm `havi-chat-v1` key exists with `{ "version": 1, "messages": [...], "updatedAt": "..." }` shape.
    - Navigate to `/app/agente` → click "Cerrar sesión" → expected: redirect to `/`.
    - DevTools → Application → Local Storage — confirm `havi-chat-v1` is GONE (Hub.listen → useSignOutClear cleared it).
    - Sign in again → /app chat home → expected: chat is reset (only intro + snapshot, no user messages from before).
    - User reports: PASS or FAIL.

    After all ten:
    - Capture results in a checklist (M-1 PASS / M-2 PASS / ... / M-10 PASS or FAIL with notes).
    - Any FAIL → halt; report failure to user; phase does NOT close. Resume after fix.
    - All PASS → proceed to Task 3.
  </how-to-verify>
  <resume-signal>
    User types "all 10 PASS" or pastes a per-flow PASS/FAIL summary. If any FAIL, user describes the issue and the executor opens a follow-up plan or fix. Approve only when M-1..M-10 are all PASS.
  </resume-signal>
  <files>(no files modified — manual flow walkthrough only)</files>
  <action>See `<what-built>` and `<how-to-verify>` above. This is a `checkpoint:human-verify` task: the executor presents the M-1..M-10 walkthrough script in `<how-to-verify>` to the user, runs `bun run dev` + `npx ampx sandbox` for the pre-flight, then pauses after each manual flow to capture PASS/FAIL + notes. The executor does NOT proceed to Task 3 until ALL TEN flows PASS per user attestation (`<resume-signal>`). ANY FAIL halts the phase.</action>
  <verify>
    <automated>MISSING — manual user attestation only. Coverage of M-1..M-10 in `<how-to-verify>` substitutes for an automated command. Pre-flight `bun run dev` boot is the only automatable check; the executor confirms it before starting M-1.</automated>
  </verify>
  <done>User has reported PASS for all ten flows (M-1 through M-10). Resume signal received per `<resume-signal>`. Per-flow PASS/FAIL notes captured for transcription into `03.1-08-SUMMARY.md` Manual gauntlet results table in Task 3. Any FAIL on any flow → phase does NOT close; this task remains blocking until fix lands.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Phase close-out — SUMMARY + STATE + ROADMAP + REQUIREMENTS update</name>
  <files>.planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-08-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/STATE.md (current state)
    - .planning/ROADMAP.md (Phase 03.1 entry — currently TBD placeholder)
    - .planning/REQUIREMENTS.md (current Validated/Active section)
    - All seven prior SUMMARY files in this phase directory if they exist (`03.1-01-SUMMARY.md` through `03.1-07-SUMMARY.md`)
    - .planning/phases/03-protected-route-auth-guard/03-03-SUMMARY.md (Phase 3 close-out template — for STATE/ROADMAP update structure analog)
  </read_first>
  <action>
    Four file updates. Order: SUMMARY first (captures what shipped); then STATE; then ROADMAP; then REQUIREMENTS.

    **1. Write `.planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-08-SUMMARY.md`** — close-out summary mirroring Phase 3's `03-03-SUMMARY.md` structure.

    Required sections (markdown headings):

    - `# Phase 03.1 Plan 08 — Close-out` + Date + Type + Status header.
    - `## What changed` — list `app/page.tsx` restyle.
    - `## Manual gauntlet results (M-1..M-10)` — table of Flow / Coverage / Result / Notes for each M-1..M-10. Replace any boilerplate "PASS or SKIPPED" with the ACTUAL outcome attested by the user in Task 2.
    - `## Final gauntlet evidence` — `bun run lint`, `typecheck`, `audit`, `build` exit codes.
    - `## Files shipped (Phase 03.1 total)` — three sub-headings:
      - NEW (~28 files) — list every new file across Plans 01-08 (amplifyTheme, all _components/, _data/, _hooks/, app/app/template.tsx, the four new sub-routes, public/icons/*).
      - MODIFIED — package.json, app/layout.tsx, app/globals.css, app/login/page.tsx, app/page.tsx, app/app/page.tsx, app/app/SignOutButton.tsx.
      - UNTOUCHED (Phase 3 contract preserved) — app/AmplifyProvider.tsx, app/app/layout.tsx, app/_components/AuthGuard.tsx, app/_components/safeFromPath.ts.
    - `## Decisions confirmed live` — map each D-42..D-52 to the M-X that proved it (M-6 Geist removal, M-7 Authenticator theme, M-8 chat flow + greeting + typing delay, M-9 slide nav + bar growth, M-10 persistence + Hub clear).
    - `## Carryover into Phase 4 (Hosting + CI)` — note the three items from CONTEXT Deferred §"Hacia Phase 4 (Hosting)" (production build verification, mock data v2 swap, sign-out placement).
    - `## Lessons learned` — 2-3 items about Plan splitting + Pitfall 6 + Pitfall 10.
    - Footer: `*Phase 03.1 Variante A — hey-banco design system + conversational HAVI — COMPLETE 2026-04-25.*`

    **2. Update `.planning/STATE.md`** — edit (not full rewrite):

    - Update `progress` block: `completed_phases: 4` (was 3), `total_plans: 21` (was 13 — adds 8), `completed_plans: 21`, `percent: 100`.
    - Update `## Current Position`: `Phase: 4`, `Plan: Not started`, `Status: Ready to plan`, `Last activity: 2026-04-25`.
    - Append to `### Decisions / Recent decisions affecting current work:` a single line summarizing Phase 03.1 close: ~28 new files, 7 modified, 4 untouched, M-1..M-10 verified, motion@12.38 + IBM Plex + Authenticator theme + chat home + 4 sub-routes + persistence + greeting all live.
    - Add row to `## Performance Metrics / By Phase`: `| 03.1 | 8 | - | - |`.
    - Append the 8 Plan 03.1-XX rows to the per-plan table at file bottom (mirror the existing Plan 02-XX rows).
    - Update `## Session Continuity`: Last session "Phase 03.1 complete (Plan 08 closed-out 2026-04-25)"; Stopped at "Phase 03.1 ship — ready to plan Phase 4"; Resume file None.
    - Update final line `**Planned Phase:** 4 (Amplify Hosting + GitHub CI) — TBD plans — pending /gsd-verify-work for Phase 03.1` (or similar wording — adjust based on actual STATE structure).

    **3. Update `.planning/ROADMAP.md`** — fill in Phase 03.1 (INSERTED) entry:

    - Replace the current TBD placeholder with:
      - **Goal:** "Aplicar el design system 'hey, banco' al scaffold y construir la Variante A conversational sobre /app: HAVI chat como motor central + sub-screens + handoff humano + alert/success/warning cards + animaciones de transición."
      - **Requirements:** "(none — UX phase; regression coverage for AUTH-01..05 via M-1..M-5)"
      - **Depends on:** "Phase 3"
      - **Plans:** "8 plans"
      - List eight `- [x] 03.1-XX-PLAN.md — <one-line summary>` entries (XX from 01 to 08, summaries matching the plans).
      - **UI hint:** yes
    - Top-of-file `## Phases` checkbox list — change Phase 03.1 line to `- [x] **Phase 03.1: Hey-banco design system + Variante A conversational HAVI (INSERTED)** - Visual replatform + conversational shell on /app`.
    - `## Progress` table — add a row: `| 03.1. Hey-banco design system + Variante A | 8/8 | Complete | 2026-04-25 |`.
    - Footer line — append `*Phase 03.1 plans created + executed: 2026-04-25*`.

    **4. Update `.planning/REQUIREMENTS.md`** — Phase 03.1 has NO new requirement IDs. Append a clarifying section:

    - After `## v1 Requirements / ### AUTH — Autenticación` block, append a sub-section `### v1 Requirements (post-Phase-03.1 visual replatform)` noting Phase 03.1 added no new IDs and listing the four AUTH IDs (01, 03, 04, 05) re-verified by manual flows M-1..M-5.
    - Update the closing footer line to reflect 2026-04-25 close: `*Last updated: 2026-04-25 — Phase 03.1 (Hey-banco design system + Variante A) shipped. AUTH-03/04/05 regression PASS via M-1..M-5; D-42..D-52 visual + UX deliverables verified live in M-6..M-10. No new requirement IDs.*`

    Run `bun run lint && bun run typecheck` (no code changes; sanity check). Then commit: prefer `gsd-sdk query commit "docs(03.1): Phase 03.1 complete — hey-banco design system + Variante A conversational HAVI"` if available, else `git add .planning/phases/03.1-... .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md` + `git commit` with the same message.
  </action>
  <verify>
    <automated>cd /home/fernando/Documents/datathon-2026 && test -f .planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-08-SUMMARY.md && grep -q "Phase 03.1" .planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-08-SUMMARY.md && grep -q "M-1" .planning/phases/03.1-hey-banco-design-system-variante-a-conversational-havi/03.1-08-SUMMARY.md && grep -q "Phase 03.1 complete" .planning/STATE.md && grep -q "03.1-01-PLAN.md" .planning/ROADMAP.md && grep -q "03.1-08-PLAN.md" .planning/ROADMAP.md && grep -q "8/8" .planning/ROADMAP.md && grep -q "Phase 03.1" .planning/REQUIREMENTS.md && bun run lint && bun run typecheck</automated>
  </verify>
  <done>
    `03.1-08-SUMMARY.md` exists with the Plan 08 close-out structure (manual gauntlet results table M-1..M-10 + final gauntlet evidence + file shipped lists + decisions confirmed live + Phase 4 carryover); `STATE.md` reflects Phase 03.1 complete (progress updated, decisions log appended, position advanced to Phase 4); `ROADMAP.md` Phase 03.1 entry filled in with goal + 8 plans listed [x] + Progress table updated to 8/8; `REQUIREMENTS.md` gains Phase 03.1 visual replatform note. `bun run lint && bun run typecheck` final pass exit 0. Phase 03.1 ships green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public home `/` → /login link | Single user-action surface; preserved Phase 2 entry point |
| Manual gauntlet → live Cognito sandbox | Real auth flows hit us-east-1_6l4dSfRCz |
| State/Roadmap/Requirements writes → planning state | Markdown updates; no code surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | Tampering | XSS via /login link or wordmark | mitigate | Wordmark renders literal `data,` text (Plan 02 component); /login link uses next/link with hardcoded `href="/login"` — no user-derived strings. |
| T-03.1-02 | Tampering | Authenticator regression | mitigate | M-1, M-2, M-3, M-7 (Task 2) verify LIVE post-Plan-02 theme + post-Plan-08 / restyle. ANY FAIL halts the phase. |
| T-03.1-03 | Elevation of Privilege | AuthGuard regression | mitigate | M-4 (Task 2) verifies LIVE: incognito → /app/grafica/comida → /login?from=%2Fapp%2Fgrafica%2Fcomida. Halts on FAIL. |
| T-03.1-04 | Information Disclosure | localStorage `havi-chat-v1` clear regression | mitigate | M-10 (Task 2) verifies LIVE: havi-chat-v1 written → present in DevTools → sign out → key gone. Halts on FAIL. |
| T-03.1-05 | Tampering | Open redirect regression | mitigate | M-4 (Task 2) cross-verifies safeFromPath via the `?from=` param URL-encoding check. |
| T-03.1-06 | Spoofing | Bundle CVE | accept | No new deps in Plan 08. Plan 01's audit already cleared motion@12.38. |
| T-03.1-07 | Denial of Service | Pattern-match catch-all bypass | mitigate | M-8 (Task 2) verifies "transferir" produces transfer-card (NOT fallback), confirming catch-all-LAST invariant lives correctly through to runtime. |
| T-03.1-08 | Information Disclosure | Mock data in production bundle | accept | Documented in Plan 08 SUMMARY's Phase 4 carryover. v2 swap-for-fetch reopens this for Hosting deploy. |
</threat_model>

<verification>
After Plan 08 ships:

```
bun run lint        # 0
bun run typecheck   # 0
bun run audit       # 0 (no ignore-list deltas vs Plan 01)
bun run build       # 0 — Phase 03.1 final BUILDABLE state
```

Plus the M-1..M-10 user-attested PASS list captured in Task 2's resume signal and recorded in 03.1-08-SUMMARY.md.

Static negatives (final phase-wide check):
- `! grep -rq "framer-motion" app/ package.json` (Pitfall 2)
- `! grep -rq "Geist" app/` (D-45 full removal)
- `! grep -rq "dangerouslySetInnerHTML" app/` (XSS protection)
- `! grep -rq "next/router" app/` (always next/navigation)
- `! grep -q "<Authenticator theme=" app/` (RESEARCH K-2 — always via ThemeProvider)
- `app/AmplifyProvider.tsx`, `app/app/layout.tsx`, `app/_components/AuthGuard.tsx`, `app/_components/safeFromPath.ts` byte-identical to Phase 3 close-out (`git diff --stat HEAD~N -- app/AmplifyProvider.tsx app/app/layout.tsx app/_components/AuthGuard.tsx app/_components/safeFromPath.ts` shows 0 changes for the Phase 03.1 commit range).
</verification>

<success_criteria>
1. `app/page.tsx` rewritten: hey palette + `<Wordmark size="lg" as="h1">` + Spanish copy ("Demo conversacional…", "Comenzar") + two `/login` links + drops next.svg/vercel.svg/dark:invert/scaffold copy.
2. Final Phase 1 gauntlet (`lint && typecheck && audit && build`) exits 0 BEFORE the manual gauntlet starts.
3. ALL TEN manual flows M-1..M-10 PASS per user attestation:
   - M-1 Sign-up + email verification (AUTH-03)
   - M-2 Sign-in + sign-out (AUTH-03)
   - M-3 Reset password (AUTH-03 — may be SKIPPED with rationale per Phase 2 02-05 pattern)
   - M-4 AuthGuard sub-route redirect (AUTH-04)
   - M-5 Session persistence (AUTH-05)
   - M-6 Visual replatform — no Geist (D-45)
   - M-7 Authenticator theme (D-44)
   - M-8 Chat home flow (D-48 + D-50 + D-51)
   - M-9 Sub-route slide nav (D-46 + D-51)
   - M-10 localStorage persistence + Hub clear (D-47)
4. `03.1-08-SUMMARY.md` written with full close-out: file shipped lists + manual gauntlet results table + decisions-confirmed-live mapping + Phase 4 carryover.
5. `STATE.md` updated: progress, completed_phases, decisions log, current position advanced to Phase 4.
6. `ROADMAP.md` Phase 03.1 entry filled in: goal text + 8 plans listed [x] + status [x] + Progress table row updated.
7. `REQUIREMENTS.md` Validated section gains the Phase 03.1 visual-replatform note.
8. Untouchable files unchanged across the entire phase: `app/AmplifyProvider.tsx`, `app/app/layout.tsx`, `app/_components/AuthGuard.tsx`, `app/_components/safeFromPath.ts`.
9. Final commit lands the four planning-doc updates + the SUMMARY (per Task 3 commit instructions).
</success_criteria>

<output>
After completion (post-user-PASS on M-1..M-10 + Task 3 commit):
- `03.1-08-SUMMARY.md` is the artifact that documents this plan's close-out work.
- `STATE.md` reads `Phase 03.1 complete` with `## Current Position / Phase: 4`.
- `ROADMAP.md` Phase 03.1 marked [x]; Phase 4 ready to plan.
- The user can now run `/gsd-plan-phase 4` to begin Amplify Hosting + GitHub CI work.

Phase 03.1 — hey-banco design system + Variante A conversational HAVI — SHIPS GREEN.
</output>
