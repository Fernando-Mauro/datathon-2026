---
phase: 02-email-password-auth-authenticator-ui
plan: 05
type: execute
wave: 5
depends_on:
  - "02-04"
files_modified:
  - .planning/phases/02-email-password-auth-authenticator-ui/02-CONTEXT.md
autonomous: false
requirements:
  - AUTH-03
  - AUTH-05
tags:
  - manual-verification
  - human-gate
  - context-update
must_haves:
  truths:
    - "D-29 flow #1 PASSES: developer signs up with a real email alias, receives the verification code (possibly from spam), enters it, and lands on /app showing their name (closes AUTH-03 sign-up + email-verify + sign-in components)"
    - "D-29 flow #2 PASSES: developer signs out from /app, returns to /, navigates to /login, signs back in with the same credentials, and lands on /app (closes AUTH-05 round-trip component)"
    - "D-29 flow #3 PASSES: post-sign-in browser refresh on /app does NOT redirect to /login; user data still present (closes AUTH-05 cookie-persistence component — validates the L-3 ssr:true behavior)"
    - "D-29 flow #4 PASSES: Forgot Password flow — developer requests reset, receives the code (possibly from spam), enters new password, signs in successfully (closes AUTH-03 reset-password component)"
    - "CONTEXT D-28 wording is updated to reflect cookie storage (per L-3 finding); the supersession is explicitly recorded for future readers"
    - "All four ROADMAP §Phase 2 success criteria are satisfied (verified in the SUMMARY)"
    - "Plan SUMMARY records the 4 manual outcomes + the D-28 wording diff for traceability"
  artifacts:
    - path: ".planning/phases/02-email-password-auth-authenticator-ui/02-CONTEXT.md"
      provides: "Updated D-28 wording reflecting cookie storage (per L-3)"
      contains: "cookies"
  key_links:
    - from: "Browser at http://localhost:3000/login"
      to: "Live Cognito User Pool (from Plan 02-04)"
      via: "<Authenticator /> form submission via aws-amplify SDK"
      pattern: "manual"
    - from: "Cognito (no-reply@verificationemail.com)"
      to: "Developer email inbox + spam folder"
      via: "Cognito default email sender"
      pattern: "manual"
    - from: ".planning/phases/02-email-password-auth-authenticator-ui/02-CONTEXT.md"
      to: "Future readers (Phase 3+ planners)"
      via: "Updated D-28 text"
      pattern: "cookies"
---

<objective>
Execute the four CONTEXT D-29 manual verification flows against the live sandbox deployed in Plan 02-04, using the frontend wired in Plan 02-03. These are HUMAN-GATED tasks — the developer at a browser with email access must drive them. They cannot be automated because they require:
1. A real (or aliased) email inbox to receive Cognito verification codes
2. A browser session to interact with the `<Authenticator>` UI
3. Multi-step click + email + click + click round-trips

After all four flows pass, update CONTEXT D-28 wording to reflect the cookie-storage finding from RESEARCH L-3 (`Amplify.configure(outputs, { ssr: true })` switches storage from localStorage to cookies — AUTH-05 still satisfied, but the wording was wrong in the original D-28).

Purpose: Closes the LIVE-VERIFICATION components of AUTH-03 + AUTH-05 (source-code components closed by Plan 02-03; backend deployment closed by Plan 02-04). Closes Phase 2.
Output: Plan SUMMARY with 4 manual outcomes + a one-line CONTEXT D-28 wording fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-email-password-auth-authenticator-ui/02-CONTEXT.md
@.planning/phases/02-email-password-auth-authenticator-ui/02-RESEARCH.md
@.planning/phases/02-email-password-auth-authenticator-ui/02-VALIDATION.md
@.planning/phases/02-email-password-auth-authenticator-ui/02-01-PLAN.md
@.planning/phases/02-email-password-auth-authenticator-ui/02-03-PLAN.md
@.planning/phases/02-email-password-auth-authenticator-ui/02-04-PLAN.md
@AGENTS.md

<interfaces>
<!-- Manual flow preconditions -->

Live state required before this plan can run (from Plan 02-04):
- CloudFormation stack `amplify-datathon2026-fernando-sandbox-0d21400c4f` in `*_COMPLETE` state
- amplify_outputs.json populated with auth block (user_pool_id, etc.)
- Cognito User Pool exists in us-east-1 / account 992839645871

Live state required before this plan can run (from Plan 02-03):
- app/AmplifyProvider.tsx mounted in app/layout.tsx with `ssr: true`
- app/login/page.tsx renders `<Authenticator />` + redirects to /app on auth
- app/app/page.tsx + SignOutButton.tsx exist
- app/page.tsx has top-right "Sign in" link

Cognito email behavior (per RESEARCH L-5):
- Default sender: `no-reply@verificationemail.com`
- Quota: 50 emails/day per AWS account (default sender)
- Often hits SPAM in Gmail / Outlook / iCloud — the developer MUST check
  the spam folder if the inbox is empty after ~2 minutes
- Real-world delivery is provider-dependent; if total non-delivery occurs,
  fallback options (per RESEARCH §"Open Questions" #5):
  (a) try a different email provider (Gmail / ProtonMail / iCloud)
  (b) configure SES (out of Phase 2 scope per CONTEXT Deferred Ideas)

CONTEXT D-28 supersession (per RESEARCH L-3):
- ORIGINAL D-28: "Amplify default es localStorage (auto-rehidrata al refrescar).
  NO custom storage adapter en v1 — los tradeoffs XSS están aceptados como
  riesgo conocido del v1 (mitigation post-MVP)."
- CORRECTED understanding: with `Amplify.configure(outputs, { ssr: true })`
  (which is REQUIRED for Next.js per official docs), token storage is
  COOKIES, not localStorage. AUTH-05 ("session persists across refresh")
  is satisfied identically — cookies survive refresh just like localStorage
  would. The XSS tradeoff acknowledged in D-28 still applies (the cookies
  are NOT HttpOnly because the SDK needs to read them).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: D-29 Flow #1 — Sign-up + email verify + sign-in (closes AUTH-03 sign-up component)</name>
  <files>(no file changes — manual browser session)</files>
  <read_first>
    - .planning/phases/02-email-password-auth-authenticator-ui/02-CONTEXT.md (D-29 #1)
    - .planning/phases/02-email-password-auth-authenticator-ui/02-VALIDATION.md (Manual-Only Verifications row 1)
    - .planning/phases/02-email-password-auth-authenticator-ui/02-RESEARCH.md L-5 (spam folder warning)
  </read_first>
  <action>
**WHAT WAS BUILT (by Plans 02-01..02-04):**
The full sign-up flow: `<Authenticator />` at /login renders a Sign-up tab
with email + name + password + confirm-password fields (Zero
Configuration); on submit, Cognito sends a 6-digit verification code to
the developer's email; on entering the code, Amplify stores the session
cookie and the useEffect-driven redirect navigates to /app showing the
user's name.

**HUMAN-ACTION REQUIRED — developer must drive this manually:**

**Preconditions (developer must confirm before starting):**

1. The dev server is running:
   ```bash
   bun run dev
   ```
   Default URL: `http://localhost:3000`

2. The developer has email access — recommended: their own email with a
   `+phase2-test1@…` alias (Gmail / iCloud / ProtonMail / Outlook all
   support `+alias` routing).

3. Cognito's 50/day email quota is fresh (developer hasn't burned through
   it on prior testing).

**Steps:**

1. Open browser → navigate to `http://localhost:3000/`. Verify the home
   page renders with the existing scaffold AND the new "Sign in" link
   top-right.

2. Click the "Sign in" link → URL becomes `/login`. Verify:
   - The `<Authenticator />` card is visible (styled — if it looks
     unstyled, the CSS import in `app/layout.tsx` is missing; STOP).
   - Tabs visible: "Sign In" (default active) + "Create Account".
   - On the "Create Account" tab, fields are: Email, Name, Password,
     Confirm Password. (If the Name field is missing, Zero Configuration
     failed to read `standard_required_attributes` from
     `amplify_outputs.json` — STOP, check `amplify_outputs.json` shape.)

3. Click "Create Account" tab. Fill in:
   - Email: `<your-email-username>+phase2-test1@<domain>` (e.g.,
     `you+phase2-test1@gmail.com`)
   - Name: `Phase 2 Test 1` (any string)
   - Password: a strong password matching Cognito default policy (≥8
     chars, upper + lower + digit + symbol — e.g., `Test1234!`)
   - Confirm Password: same as Password

4. Click "Create Account" button. Expected:
   - Form transitions to a "Confirm sign up" / "Verify your email"
     screen showing a code input.
   - Cognito has sent a 6-digit code to your email.

5. **Check your email inbox AND your spam folder.** The sender will be
   `no-reply@verificationemail.com`. Subject typically "Your verification
   code". (Per L-5, this often hits spam — give it up to 2 minutes,
   then check spam.)

6. Copy the 6-digit code. Paste it into the verification screen. Click
   "Confirm" / "Submit".

7. Expected behavior:
   - The Authenticator transitions to an "authenticated" state briefly.
   - The useEffect in `app/login/page.tsx` fires `router.push("/app")`.
   - Browser URL changes to `http://localhost:3000/app`.
   - Page renders: "Welcome" heading + paragraph + "Sign out" button.

**Pass criteria:**
   - You can sign up with the alias.
   - You receive the verification code email (in inbox OR spam).
   - Entering the code succeeds (no "invalid code" error).
   - You land on `/app` automatically.
   - The page shows "Welcome".

**Fallback if email never arrives** (per RESEARCH §"Open Questions" #5):
   - Wait at least 2 minutes — initial deliverability is sometimes slow.
   - Check spam folder thoroughly (the sender domain is spam-prone).
   - If still nothing after 5 minutes, try a different email provider
     (Gmail / ProtonMail / iCloud each have different filter behaviors).
   - If still nothing across multiple providers, the SES escape hatch
     is OUT OF SCOPE for Phase 2 — pause Phase 2 and open a follow-up.

**RESUME SIGNAL — developer types one of:**
- `passed: flow 1 — <alias used>` (e.g., `passed: flow 1 — you+phase2-test1@gmail.com`)
- `failed: flow 1 — <reason>` (e.g., `failed: flow 1 — code never arrived after 10 min, tried gmail and proton`)
  </action>
  <verify>
    <automated>MANUAL — developer-confirmed via resume-signal text. No automated check possible (requires real email + browser session). Acceptance criteria validate the developer's confirmation rather than a script's output.</automated>
  </verify>
  <acceptance_criteria>
    - Developer confirms sign-up succeeded with the alias they used (recorded in resume-signal text)
    - Developer confirms the verification email arrived (inbox or spam — both count)
    - Developer confirms entering the code succeeded (no "invalid code" error)
    - Developer confirms automatic redirect to `/app` after code entry (no manual nav needed)
    - Developer confirms the `/app` page renders the "Welcome" heading + Sign out button
    - The email alias used is recorded in the SUMMARY for the next flows (re-used in Flow #2)
  </acceptance_criteria>
  <done>Developer confirms D-29 flow #1 passed; the alias used + the live `user_pool_id` + the timing (deliverability latency) are recorded in the plan SUMMARY.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: D-29 Flow #2 — Sign-out + re-sign-in round-trip (closes AUTH-05 round-trip component)</name>
  <files>(no file changes — manual browser session)</files>
  <read_first>
    - .planning/phases/02-email-password-auth-authenticator-ui/02-CONTEXT.md (D-29 #2, D-27)
    - .planning/phases/02-email-password-auth-authenticator-ui/02-VALIDATION.md (Manual-Only Verifications row 2)
    - The recorded email alias from Task 1's resume-signal
  </read_first>
  <action>
**WHAT WAS BUILT:**
The sign-out + re-sign-in cycle: clicking the Sign out button on /app
awaits signOut() (clears the cookie), then router.push("/") returns to
home. Navigating to /login and signing back in with the SAME
credentials succeeds and redirects to /app again — proving the user
record persisted in Cognito and credentials work.

**HUMAN-ACTION REQUIRED — developer must drive this manually:**

**Precondition:** Task 1 passed. Browser is currently on `/app` (signed in
as `you+phase2-test1@…`).

**Steps:**

1. On the `/app` page, click the "Sign out" button (outlined button at the
   bottom of the page).

2. Expected: brief moment while `signOut()` resolves, then browser
   navigates to `http://localhost:3000/` (home page). The "Sign in" link
   top-right is still visible.

3. Open DevTools → Application → Cookies → `http://localhost:3000`.
   Expected: the `CognitoIdentityServiceProvider.*` cookies set by Amplify
   are now CLEARED or significantly reduced (the access token cookie is
   gone). This is optional sanity verification — the next step is the
   real proof.

4. Click the "Sign in" link top-right → URL becomes `/login`. The
   `<Authenticator />` shows the Sign In tab (default).

5. Enter:
   - Email: the SAME alias from Task 1 (e.g.,
     `you+phase2-test1@gmail.com`)
   - Password: the SAME password from Task 1

6. Click "Sign in" button. Expected:
   - NO email verification step (the user is already verified from Task 1).
   - The Authenticator transitions to authenticated state.
   - useEffect fires `router.push("/app")`.
   - Browser URL changes to `/app`.
   - Welcome page renders.

**Pass criteria:**
   - Sign out from /app works (lands on home `/`).
   - Re-sign-in with the same credentials works (no need to re-verify email).
   - Lands on /app after re-sign-in.

**Failure modes:**
   - "User does not exist" on re-sign-in → the user wasn't actually
     persisted (Cognito error or email typo). Re-check the alias.
   - "Incorrect username or password" → typo on the password. Try again.
   - Sign-out throws "no auth config" → the AmplifyProvider didn't run on
     /app (L-2 race). This means Plan 02-03 Task 1 / Task 2 is broken —
     STOP and re-verify the root layout mounts AmplifyProvider.

**RESUME SIGNAL — developer types one of:**
- `passed: flow 2`
- `failed: flow 2 — <reason>`
  </action>
  <verify>
    <automated>MANUAL — developer-confirmed via resume-signal text. No automated check possible (requires real browser session + Cognito interaction). Acceptance criteria validate the developer's confirmation.</automated>
  </verify>
  <acceptance_criteria>
    - Developer confirms sign-out worked (landed on `/`)
    - Developer confirms re-sign-in worked (no email verification needed; landed on `/app`)
    - Developer optionally confirms via DevTools that cookies were cleared after sign-out and re-set after re-sign-in
    - No "no auth config" / "AuthTokenConfigException" errors thrown (proves AmplifyProvider is at root and works for /app)
  </acceptance_criteria>
  <done>Developer confirms D-29 flow #2 passed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: D-29 Flow #3 — Refresh persistence (closes AUTH-05 cookie-persistence component, validates L-3 ssr:true behavior)</name>
  <files>(no file changes — manual browser session)</files>
  <read_first>
    - .planning/phases/02-email-password-auth-authenticator-ui/02-CONTEXT.md (D-29 #3, D-28 — note D-28 wording will be corrected in Task 5)
    - .planning/phases/02-email-password-auth-authenticator-ui/02-VALIDATION.md (Manual-Only Verifications row 3)
    - .planning/phases/02-email-password-auth-authenticator-ui/02-RESEARCH.md L-3 (ssr:true → cookies)
  </read_first>
  <action>
**WHAT WAS BUILT:**
The cookie-based session persistence: Amplify.configure(outputs, { ssr:
true }) stores the session in cookies (NOT localStorage as CONTEXT D-28
originally claimed); cookies survive a browser refresh; the Authenticator
rehydrates the session on mount (transient "configuring" state) and
reports authStatus === "authenticated" without requiring re-login.

**HUMAN-ACTION REQUIRED — developer must drive this manually:**

**Precondition:** Task 2 passed. Browser is currently on `/app` (signed in).

**Steps:**

1. With browser on `http://localhost:3000/app`, hit Refresh (Ctrl+R on
   Linux/Windows, Cmd+R on Mac, or click the browser refresh button).

2. Expected:
   - Brief moment showing nothing or the Next.js loading boundary.
   - Page re-renders the `/app` Welcome content.
   - URL stays at `/app` — does NOT redirect to `/login`.
   - The Sign out button is present (proves the AmplifyProvider hydrated
     and the user is still signed in).

3. **Optional sanity** — open DevTools → Application → Cookies →
   `http://localhost:3000`. You should see cookies named like:
   - `CognitoIdentityServiceProvider.<client_id>.LastAuthUser`
   - `CognitoIdentityServiceProvider.<client_id>.<username>.accessToken`
   - `CognitoIdentityServiceProvider.<client_id>.<username>.idToken`
   - `CognitoIdentityServiceProvider.<client_id>.<username>.refreshToken`

   The presence of these cookies (NOT in localStorage — DevTools →
   Application → Local Storage will be empty for this origin) confirms
   the L-3 finding: `ssr: true` switched storage to cookies, not the
   localStorage that CONTEXT D-28 originally assumed.

4. **Optional secondary check** — close the tab entirely. Open a NEW tab.
   Navigate to `http://localhost:3000/app`. Expected: still signed in
   (cookies persist across tabs in the same browser session).

**Pass criteria:**
   - Refresh on `/app` does NOT redirect to `/login`.
   - The user remains signed in (Welcome page still renders, Sign out
     button still visible).
   - (Optional) DevTools confirms cookies in Cookies, not localStorage.

**Failure modes:**
   - Refresh redirects to `/login` → either ssr:true wasn't set in
     AmplifyProvider, OR the cookies were rejected (browser cookie
     restrictions, e.g., 3rd-party cookie blocking on a sensitive
     domain). For localhost development, cookies should always work.
     If failing, re-check `app/AmplifyProvider.tsx` for `{ ssr: true }`.
   - Refresh shows "no auth config" error → AmplifyProvider didn't mount
     on /app (L-2 race). Re-verify root layout.

**RESUME SIGNAL — developer types one of:**
- `passed: flow 3` (and optionally append `cookies confirmed` / `localStorage empty` if the DevTools check was done)
- `failed: flow 3 — <reason>`
  </action>
  <verify>
    <automated>MANUAL — developer-confirmed via resume-signal text. No automated check possible (requires real browser session + cookie observation). Acceptance criteria validate the developer's confirmation.</automated>
  </verify>
  <acceptance_criteria>
    - Developer confirms browser refresh on `/app` did NOT redirect to `/login`
    - Developer confirms the Welcome page + Sign out button still render after refresh
    - (Optional) Developer confirms via DevTools that the session is in COOKIES (not localStorage) — this is the L-3 supersession evidence
  </acceptance_criteria>
  <done>Developer confirms D-29 flow #3 passed; AUTH-05 closed via cookie persistence (the L-3 finding is empirically confirmed if the optional DevTools check was done).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: D-29 Flow #4 — Forgot Password reset flow (closes AUTH-03 reset-password component)</name>
  <files>(no file changes — manual browser session)</files>
  <read_first>
    - .planning/phases/02-email-password-auth-authenticator-ui/02-CONTEXT.md (D-29 #4, D-19 — accountRecovery: EMAIL_ONLY)
    - .planning/phases/02-email-password-auth-authenticator-ui/02-VALIDATION.md (Manual-Only Verifications row 4)
    - .planning/phases/02-email-password-auth-authenticator-ui/02-RESEARCH.md L-5 (spam folder warning)
    - The recorded email alias from Task 1
  </read_first>
  <action>
**WHAT WAS BUILT:**
The Forgot Password flow: `<Authenticator />` exposes a "Forgot your
password?" link on the Sign In tab. Clicking it opens a flow where the
user enters their email; Cognito sends a 6-digit reset code; user
enters the code + a new password; on success, user can sign in with
the new password. accountRecovery: "EMAIL_ONLY" (per D-19) is what
enables this.

**HUMAN-ACTION REQUIRED — developer must drive this manually:**

**Precondition:** Task 3 passed. The user `you+phase2-test1@…` is
registered in Cognito.

**Steps:**

1. From wherever you are, navigate to `http://localhost:3000/login`. (If
   currently signed in, sign out first via the /app page's Sign out
   button, OR open an incognito / private window to start clean.)

2. On the Sign In tab, click the "Forgot your password?" link (typically
   below the password field).

3. Enter the email alias from Task 1 (e.g., `you+phase2-test1@gmail.com`).
   Click "Send code" / "Submit".

4. Expected: Cognito sends a 6-digit code to the email. Form transitions
   to a "Reset password" screen with two fields: Code + New Password (and
   often a Confirm Password).

5. **Check your email inbox AND your spam folder** for the reset code
   (per L-5, may hit spam).

6. Enter the code + a NEW password (different from the original Task 1
   password; must satisfy Cognito default policy: ≥8 chars + upper +
   lower + digit + symbol — e.g., `NewPass123!`). Confirm Password if
   prompted. Click "Submit" / "Reset".

7. Expected: form transitions back to the Sign In tab (or directly
   authenticates).

8. If returned to Sign In: enter the email + the NEW password. Click
   "Sign in".

9. Expected: success → redirect to `/app`.

**Pass criteria:**
   - "Forgot your password?" link visible on the Sign In tab.
   - Reset code email arrives (inbox OR spam — both count).
   - Entering the code + new password succeeds.
   - Sign-in with the NEW password succeeds → lands on `/app`.

**Failure modes:**
   - "Forgot your password?" link missing → `accountRecovery: "EMAIL_ONLY"`
     wasn't set OR the deploy didn't pick up the value. Re-check
     `amplify/auth/resource.ts` and re-deploy.
   - Reset code email never arrives → same fallback as Task 1 (check spam,
     try a different provider).
   - Sign-in with new password fails ("Incorrect username or password") →
     either typo on the new password, OR the reset didn't actually
     commit. Repeat the reset.

**Note on Cognito email quota** (per L-5): you've now sent at least 2
emails today (Task 1 verification + Task 4 reset). The 50/day quota is
shared across the AWS account; plenty of headroom for a hackathon dev,
but worth knowing if rapid iteration burns through it.

**RESUME SIGNAL — developer types one of:**
- `passed: flow 4` (and optionally `new password set successfully`)
- `failed: flow 4 — <reason>`
  </action>
  <verify>
    <automated>MANUAL — developer-confirmed via resume-signal text. No automated check possible (requires real email + multi-step browser flow). Acceptance criteria validate the developer's confirmation.</automated>
  </verify>
  <acceptance_criteria>
    - Developer confirms the "Forgot your password?" link is visible (proves accountRecovery: EMAIL_ONLY took effect)
    - Developer confirms the reset code email arrived (inbox or spam)
    - Developer confirms entering the code + new password succeeded
    - Developer confirms signing in with the NEW password succeeded → landed on `/app`
  </acceptance_criteria>
  <done>Developer confirms D-29 flow #4 passed; AUTH-03 fully closed (sign-up + email verify + sign-in + sign-out + reset password all live and working against the sandbox Cognito).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Update CONTEXT D-28 wording to reflect cookie storage (per L-3 supersession)</name>
  <files>.planning/phases/02-email-password-auth-authenticator-ui/02-CONTEXT.md</files>
  <read_first>
    - .planning/phases/02-email-password-auth-authenticator-ui/02-CONTEXT.md (find current D-28 text, lines ~78)
    - .planning/phases/02-email-password-auth-authenticator-ui/02-RESEARCH.md L-3 (the cited finding)
    - .planning/phases/02-email-password-auth-authenticator-ui/02-03-PLAN.md §"Interfaces" §"CRITICAL token storage finding" (the supersession is already documented in plan 03)
  </read_first>
  <behavior>
    - The D-28 paragraph in 02-CONTEXT.md is updated to reflect that storage
      is cookies (per L-3), NOT localStorage.
    - The original wording (localStorage) is preserved for traceability,
      annotated with the supersession reason.
    - AUTH-05 is explicitly noted as still satisfied (cookies survive
      refresh just like localStorage would).
    - The XSS tradeoff acknowledgment is preserved (the cookies are NOT
      HttpOnly because the SDK needs to read them — same XSS exposure as
      localStorage would have had).
    - No other D-* decisions are touched.
  </behavior>
  <action>
**Locate the D-28 block** in 02-CONTEXT.md. The current text (verbatim from
lines ~78):

```markdown
- **D-28:** Persistencia de sesión: Amplify default es localStorage (auto-rehidrata al refrescar). NO custom storage adapter en v1 — los tradeoffs XSS están aceptados como riesgo conocido del v1 (mitigation post-MVP).
```

**Replace it with the corrected version (Spanish to match surrounding text;
includes the supersession note pointing back to L-3):**

```markdown
- **D-28:** Persistencia de sesión: tokens guardados en **cookies** (no localStorage). Esto es porque `Amplify.configure(outputs, { ssr: true })` — requerido por la docs oficial de Amplify para Next.js — cambia el storage backend a cookies (las cookies sobreviven al refresh igual que localStorage lo haría → AUTH-05 cumplido). NO custom storage adapter en v1. Las cookies NO son HttpOnly (el SDK necesita leerlas), así que los tradeoffs XSS siguen aceptados como riesgo conocido del v1 — mismo perfil que la wording original mencionaba para localStorage. Mitigation post-MVP. **(Esta wording corrige la D-28 original que asumía localStorage; ver RESEARCH §"Gotchas" L-3 + 02-03-PLAN §"Interfaces" para la cita oficial.)**
```

**Why this wording:**
- "tokens guardados en **cookies** (no localStorage)" — direct correction
  upfront, calls out the change.
- Cites `Amplify.configure(outputs, { ssr: true })` as the cause (the
  user can grep `app/AmplifyProvider.tsx` to confirm).
- Notes "AUTH-05 cumplido" explicitly (cookies satisfy the requirement
  the same way localStorage would have).
- Preserves the XSS tradeoff acknowledgment (the cookies are not
  HttpOnly, so the XSS risk profile is unchanged from what D-28
  originally documented — the original wording wasn't WRONG about the
  risk, just wrong about the mechanism).
- Final parenthetical points future readers to the source of truth
  (RESEARCH L-3 + the 02-03-PLAN interfaces block) for the supersession.

**Verification after the edit:**

```bash
grep -q '\*\*D-28:\*\* Persistencia de sesión: tokens guardados en \*\*cookies\*\*' .planning/phases/02-email-password-auth-authenticator-ui/02-CONTEXT.md \
  && grep -q 'AUTH-05 cumplido' .planning/phases/02-email-password-auth-authenticator-ui/02-CONTEXT.md \
  && grep -q 'L-3' .planning/phases/02-email-password-auth-authenticator-ui/02-CONTEXT.md
```

All three must exit 0.

**Forbidden:**
- Do NOT delete the original D-28 paragraph WITHOUT replacement (the
  decision ID D-28 must remain referenceable).
- Do NOT touch any other D-* decision (only D-28 is affected by L-3).
- Do NOT remove the XSS tradeoff acknowledgment — it's still
  load-bearing (the storage mechanism changed, but the risk profile
  did NOT improve).
- Do NOT rewrite the file from scratch — surgical edit on the D-28
  paragraph only.
- Do NOT change any other section of 02-CONTEXT.md (Boundary, other
  Decisions, Code Context, Specifics, Deferred, Canonical Refs all
  stay as-is).
- Do NOT commit `amplify_outputs.json` even if it shows up in
  `git status` (still gitignored per Phase 1 G-8).
  </action>
  <verify>
    <automated>grep -q 'tokens guardados en \*\*cookies\*\*' .planning/phases/02-email-password-auth-authenticator-ui/02-CONTEXT.md &amp;&amp; grep -q 'AUTH-05 cumplido' .planning/phases/02-email-password-auth-authenticator-ui/02-CONTEXT.md &amp;&amp; grep -q 'L-3' .planning/phases/02-email-password-auth-authenticator-ui/02-CONTEXT.md &amp;&amp; ! grep -q 'Amplify default es localStorage (auto-rehidrata al refrescar). NO custom storage' .planning/phases/02-email-password-auth-authenticator-ui/02-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-email-password-auth-authenticator-ui/02-CONTEXT.md` contains literal `tokens guardados en **cookies**` (the corrected wording)
    - File contains literal `AUTH-05 cumplido` (the requirement-still-met note)
    - File contains literal `L-3` (the source-of-truth pointer)
    - File no longer contains the original "Amplify default es localStorage (auto-rehidrata al refrescar). NO custom storage" sentence
    - All other D-* decisions (D-15..D-27, D-29) are unchanged
    - Other CONTEXT sections (Phase Boundary, Code Context, Specifics, Deferred, Canonical Refs, Discussion-Log header) are unchanged
  </acceptance_criteria>
  <done>D-28 wording corrected to reflect cookie storage (per L-3); supersession is explicit and cites the source. No other CONTEXT content modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Cognito (over HTTPS) | All four manual flows exercise this boundary. The `<Authenticator>` posts credentials directly to the Cognito User Pool. |
| Cognito → Email provider (Gmail/iCloud/etc.) | Verification + reset codes traverse this boundary via Cognito's default sender (`no-reply@verificationemail.com`). |
| Email provider → Developer's inbox | Final hop. Filtering decisions (spam vs inbox) are the email provider's. |
| Browser ↔ disk (cookie storage) | Token cookies persist across page reloads via the browser's cookie store. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-05-01 | I (Information Disclosure) | Verification code in email body | accept | Cognito's default 6-digit code in plaintext email is the standard pattern; account takeover risk is bounded by inbox security. Hackathon-acceptable per D-19. |
| T-2-05-02 | D (Denial of Service) | Cognito 50/day email quota | accept | Documented in L-5; flow instructions warn the developer. Real usage is well below cap; SES move is post-MVP. |
| T-2-05-03 | T (Tampering) | Email never arrives → can't complete sign-up | accept (with documented fallback) | Per Open Question #5 fallback: try a different email provider. Total non-delivery across providers triggers Phase pause + SES escape hatch (out of scope). |
| T-2-05-04 | I (Information Disclosure) | Token cookies are NOT HttpOnly | accept | Required for SDK to function (per L-3); same XSS exposure as localStorage would have had. Documented in updated D-28 wording. Mitigation (HttpOnly + server-side proxy via `@aws-amplify/adapter-nextjs`) is post-MVP / Phase 4 evaluation. |
| T-2-05-05 | T (Tampering) | Manual flow false-pass (developer says "passed" but didn't actually verify) | mitigate (procedural) | Each task's `acceptance_criteria` lists specific observable outcomes (URL changes, button visibility, no error messages). Resume-signal includes the alias used in flow 1 (re-used in flows 2 and 4) so the SUMMARY can cross-reference. The `checkpoint:human-action` type ensures the developer actively types a confirmation rather than passively passing through. |
| T-2-05-06 | E (Elevation of Privilege) | CONTEXT D-28 supersession introduces audit-trail risk | mitigate | Task 5 preserves the supersession EXPLICITLY in the new D-28 wording (cites L-3 + 02-03-PLAN). Future readers (Phase 3+ planners) see both the corrected wording AND the pointer to where the change came from. |
</threat_model>

<verification>
After all 5 tasks complete (4 manual + 1 CONTEXT edit), the plan SUMMARY
must record:

1. The email alias used (from Task 1).
2. The 4 D-29 manual flow outcomes ("passed" or "failed: <reason>" for each).
3. (Optional) DevTools cookie observation from Task 3 confirming cookies are used (validates L-3).
4. The before/after wording of D-28 (Task 5).
5. Confirmation that `bun run lint && bun run typecheck && bun run build && bun run audit` still all exit 0 (the dev server may be running, but the production build pipeline is unchanged).

ROADMAP §Phase 2 success criteria cross-check (all four must be ✓):
- ☐ `amplify/auth/resource.ts` declares `defineAuth({ loginWith: { email: true } })` and is deployed to the sandbox → ✓ (Plan 02-01 + 02-04)
- ☐ The app wraps the React tree with `<Authenticator>` (or equivalent) and shows sign-up + email-verify + sign-in + sign-out flows → ✓ (Plan 02-03 + Tasks 1+2 of 02-05)
- ☐ After sign-in, browser refresh keeps the user logged in (no return to login) → ✓ (Task 3 of 02-05)
- ☐ The user can initiate password reset from the UI and complete the email-code flow → ✓ (Task 4 of 02-05)
</verification>

<success_criteria>
- All four D-29 manual flows pass (developer confirms each via resume-signal).
- CONTEXT D-28 wording updated to reflect cookie storage (per L-3 supersession), with the new wording explicitly citing the source.
- All four ROADMAP §Phase 2 success criteria are ✓.
- AUTH-03 + AUTH-05 fully closed.
- Phase 2 complete (5/5 plans).
- The plan SUMMARY records the manual outcomes for traceability.
</success_criteria>

<output>
After completion, create `.planning/phases/02-email-password-auth-authenticator-ui/02-05-SUMMARY.md` documenting:
- The email alias used (Task 1).
- The 4 manual flow outcomes (each "passed" or "failed: <reason>").
- (Optional) DevTools cookie observation from Task 3 (yes / no — confirms or doesn't confirm L-3).
- The before/after wording of D-28 (Task 5 diff).
- Cross-check against ROADMAP §Phase 2 success criteria (all 4 must be ✓ for the phase to be complete).
- Final repo gate exit codes (lint / typecheck / build / audit — all should still be 0).
- Phase 2 closure note: "AUTH-01 closed (Plan 02-01 source + 02-04 deploy), AUTH-03 closed (Plan 02-03 source + 02-05 manual), AUTH-05 closed (Plan 02-03 source + 02-05 manual flow #3 cookie-persistence)."
- Optional follow-up note for Phase 3 readers: per the L-3 supersession, when Phase 4 evaluates `@aws-amplify/adapter-nextjs` for the route guard, the cookie-storage choice from Phase 2 is the relevant context.
</output>
