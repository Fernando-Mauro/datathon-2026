---
phase: 01-foundation-amplify-backend-skeleton
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - package.json
  - bun.lock
  - tsconfig.json
  - amplify/backend.ts
  - amplify/package.json
  - amplify/tsconfig.json
autonomous: true
requirements:
  - INFRA-01
tags:
  - amplify
  - backend
  - skeleton
  - cdk
  - manual-install
  - bun

must_haves:
  truths:
    - "`amplify/backend.ts` exists with the literal call `defineBackend({})` and no resources"
    - "`amplify/package.json` declares ESM via `{ \"type\": \"module\" }`"
    - "`amplify/tsconfig.json` configures ES2022/strict + the `$amplify/*` path alias"
    - "Root `tsconfig.json` excludes `amplify/**/*` from the Next compile"
    - "`@aws-amplify/backend`, `@aws-amplify/backend-cli`, `aws-cdk-lib`, `constructs`, `tsx`, `esbuild` are devDependencies"
    - "`bun run build`, `bun run lint`, `bun run typecheck`, and `bun run audit` still exit 0 after Amplify is installed"
    - "Backend TS type-checks via `cd amplify && npx tsc --noEmit` (no AWS network calls required)"
  artifacts:
    - path: amplify/backend.ts
      provides: "Bare CDK entry point — `defineBackend({})`"
      min_lines: 5
      contains: "defineBackend({})"
    - path: amplify/package.json
      provides: "ESM declaration so `tsx` loads `backend.ts` as ES module"
      contains: "\"type\": \"module\""
    - path: amplify/tsconfig.json
      provides: "Backend-only TS config with `$amplify/*` path alias"
      contains: "$amplify/*"
    - path: tsconfig.json
      provides: "Root TS config now excludes `amplify/**/*` so Next does not try to compile backend code (G-6)"
      contains: "amplify/**/*"
    - path: package.json
      provides: "Amplify devDeps: backend, backend-cli, aws-cdk-lib, constructs, tsx, esbuild"
      contains: "@aws-amplify/backend"
  key_links:
    - from: amplify/backend.ts
      to: "@aws-amplify/backend"
      via: "named import of `defineBackend`"
      pattern: "from \"@aws-amplify/backend\""
    - from: amplify/tsconfig.json
      to: ".amplify/generated/*"
      via: "`$amplify/*` path alias (used by Phase 2+ generated typedefs)"
      pattern: "\\$amplify"
    - from: tsconfig.json
      to: amplify directory
      via: "`exclude` array contains `amplify/**/*` so Next's TS compilation skips backend code (G-6)"
      pattern: "amplify/\\*\\*/\\*"
---

<objective>
Plant the Amplify Gen 2 backend skeleton (INFRA-01) using the **manual installation** path (NOT `npm create amplify@latest`). Per RESEARCH §3 and gotchas G-3/G-4, the official scaffolder is incompatible with Bun and ships an `auth/`+`data/` template that violates D-05/D-06. Manual install gives the truly-empty `defineBackend({})` the CONTEXT requires. This is Wave C from RESEARCH §"Recommended task wave ordering": still no AWS network calls — that comes in plan 05.

Purpose: This plan is what makes the Amplify Gen 2 toolchain physically present in the repo. After this plan, all the backend-side artifacts exist locally and type-check; plan 04 documents how to use them, and plan 05 actually deploys them to AWS.

Output: 6 file changes — 3 new files in `amplify/` (`backend.ts`, `package.json`, `tsconfig.json`), root `tsconfig.json` exclude updated, `package.json` devDeps extended, `bun.lock` refreshed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-amplify-backend-skeleton/01-CONTEXT.md
@.planning/phases/01-foundation-amplify-backend-skeleton/01-RESEARCH.md
@.planning/phases/01-foundation-amplify-backend-skeleton/01-VALIDATION.md
@.planning/phases/01-foundation-amplify-backend-skeleton/01-PATTERNS.md
@.planning/phases/01-foundation-amplify-backend-skeleton/01-01-SUMMARY.md
@.planning/phases/01-foundation-amplify-backend-skeleton/01-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@AGENTS.md
@package.json
@tsconfig.json

<interfaces>
<!-- Existing root `tsconfig.json` `exclude` array (line 33) -->
```json
"exclude": ["node_modules"]
```

<!-- After this plan -->
```json
"exclude": ["node_modules", "amplify/**/*"]
```

<!-- Existing `package.json` devDependencies (alphabetical, exact-or-caret per existing entries):
  @tailwindcss/postcss, @types/node, @types/react, @types/react-dom, eslint, eslint-config-next, tailwindcss, typescript -->

<!-- After this plan, the new alphabetical positions for the Amplify entries:
  @aws-amplify/backend         → before @tailwindcss/postcss
  @aws-amplify/backend-cli     → before @tailwindcss/postcss
  aws-cdk-lib                  → after @types/react-dom, before eslint
  constructs                   → after @types/react-dom (or after aws-cdk-lib), before eslint
  esbuild                      → after eslint-config-next, before tailwindcss
  tsx                          → after tailwindcss (or before typescript)

  Bun's package manager will sort alphabetically on install regardless; no need to hand-place perfectly. -->

<!-- Bare amplify/backend.ts contract -->
```typescript
import { defineBackend } from "@aws-amplify/backend";

defineBackend({}); // empty record — D-05/D-06 require no auth/data/storage in Phase 1
```

<!-- amplify/package.json shape (verbatim per Amplify create-amplify source) -->
```json
{ "type": "module" }
```

<!-- amplify/tsconfig.json shape (verbatim per Amplify create-amplify source) -->
{ "compilerOptions": { "target": "es2022", "module": "es2022", "moduleResolution": "bundler", ..., "paths": { "$amplify/*": ["../.amplify/generated/*"] } } }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install Amplify Gen 2 devDependencies via Bun (manual install — NOT `npm create amplify`)</name>
  <files>package.json, bun.lock</files>
  <read_first>
    - /home/fernando/Documents/datathon-2026/package.json (current devDependencies block)
    - /home/fernando/Documents/datathon-2026/.planning/phases/01-foundation-amplify-backend-skeleton/01-RESEARCH.md §3 ("Plant the Amplify Gen 2 backend skeleton", lines 218-300) — especially §3.a (install command, line 240-247) and the "Critical decision: do NOT use `npm create amplify@latest`" preamble (lines 220-238)
    - /home/fernando/Documents/datathon-2026/.planning/phases/01-foundation-amplify-backend-skeleton/01-RESEARCH.md "Gotchas & Landmines" G-3 (create-amplify incompatible with Bun) and G-4 (basic-auth-data template violates D-05/D-06), G-10 (use `npx ampx`, not `bunx`)
    - /home/fernando/Documents/datathon-2026/.planning/phases/01-foundation-amplify-backend-skeleton/01-RESEARCH.md §"Stack & Versions" → "To add / modify in Phase 1" table (lines 124-138)
    - /home/fernando/Documents/datathon-2026/.planning/phases/01-foundation-amplify-backend-skeleton/01-CONTEXT.md decisions D-05, D-06
    - /home/fernando/Documents/datathon-2026/.planning/phases/01-foundation-amplify-backend-skeleton/01-PATTERNS.md "package.json (MODIFY ...)" §"Core pattern — devDependencies additions" (lines 348-358)
  </read_first>
  <action>
**DO NOT** run `npm create amplify@latest`, `npx create-amplify@latest`, `bun create amplify@latest`, or `bunx create-amplify` — per RESEARCH G-3 the scaffolder throws `UnsupportedPackageManagerError` on Bun, and per G-4 it ships an `auth/` + `data/` template that violates locked decisions D-05/D-06. The manual install path is the only correct choice.

From the repo root, run EXACTLY:

```bash
bun add -d @aws-amplify/backend@latest @aws-amplify/backend-cli@latest aws-cdk-lib@2.244.0 constructs@^10.0.0 tsx esbuild
```

Then defensively re-install (G-14 pattern from RESEARCH §1):

```bash
bun install
```

Verify the lockfile picked up the new entries:

```bash
bun pm ls | grep -E "@aws-amplify/(backend|backend-cli)|aws-cdk-lib|constructs|^tsx|^esbuild" | head
```

Hard rules:
- All six packages go to `devDependencies` (the `-d` / `--dev` flag).
- `aws-cdk-lib` is pinned to **exactly** `2.244.0` per RESEARCH §3.a (matches what the scaffolder would have written via `default_packages.json`).
- `tsx` and `esbuild` use `latest` because Amplify's own scaffolder doesn't pin them.
- DO NOT install `aws-amplify` (the frontend client) — RESEARCH §3.a explicitly defers this to Phase 2 when `<Authenticator>` is wired.
- DO NOT install `postcss` as a top-level devDep unless plan 01's defensive fallback was triggered (recorded in 01-01-SUMMARY.md). Plan 01 owns the postcss decision.
- Preserve all existing `dependencies`, `scripts`, `ignoreScripts`, `trustedDependencies` blocks. The only changes Bun should make to `package.json` are inside the `devDependencies` block (Bun will sort alphabetically on save).

After install, smoke-test that the existing scaffold still builds:

```bash
bun run lint
bun run typecheck
bun run build
bun run audit
```

All MUST exit 0. Adding Amplify devDeps should NOT regress any of these gates. If `bun run audit` reports a NEW advisory introduced by an Amplify transitive, STOP and surface it for triage — do not bypass.
  </action>
  <verify>
    <automated>node -e "const d=require('./package.json').devDependencies; ['@aws-amplify/backend','@aws-amplify/backend-cli','aws-cdk-lib','constructs','tsx','esbuild'].forEach(p=>{if(!d[p])throw new Error('missing '+p)}); if(d['aws-cdk-lib']!=='2.244.0')throw new Error('aws-cdk-lib must be exactly 2.244.0, got '+d['aws-cdk-lib']); if(d['aws-amplify'])throw new Error('aws-amplify (frontend client) MUST NOT be installed in Phase 1'); console.log('ok')" &amp;&amp; ! test -f package-lock.json &amp;&amp; bun run lint &amp;&amp; bun run typecheck &amp;&amp; bun run build &amp;&amp; bun run audit</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `devDependencies` contains `@aws-amplify/backend` (any `^1.x.x` range >= 1.22.0).
    - `package.json` `devDependencies` contains `@aws-amplify/backend-cli` (any `^1.x.x` range >= 1.8.2).
    - `package.json` `devDependencies` contains `aws-cdk-lib` pinned to exactly `2.244.0` (no caret, no tilde).
    - `package.json` `devDependencies` contains `constructs` (`^10.0.0` or compatible).
    - `package.json` `devDependencies` contains `tsx` and `esbuild`.
    - `package.json` `devDependencies` does NOT contain `aws-amplify` (frontend client; Phase 2 owns it).
    - `package-lock.json` does NOT exist (G-3 — running create-amplify via npx would have written one; we explicitly don't).
    - `bun pm ls` lists all six new packages with concrete resolved versions.
    - `bun run lint` exits 0.
    - `bun run typecheck` exits 0.
    - `bun run build` exits 0.
    - `bun run audit` exits 0 (CVE gate from plan 01 still green; Amplify transitives introduce no new moderate-or-higher advisories).
    - `dependencies`, `scripts`, `ignoreScripts`, `trustedDependencies` blocks in `package.json` are unchanged from the end of plan 01.
  </acceptance_criteria>
  <done>Six Amplify Gen 2 devDeps are installed via Bun (NOT via `npm create amplify`); existing build/lint/typecheck/audit gates still green; no `package-lock.json` artifact appeared.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create amplify/{backend.ts,package.json,tsconfig.json} and exclude amplify from root tsconfig</name>
  <files>amplify/backend.ts, amplify/package.json, amplify/tsconfig.json, tsconfig.json</files>
  <read_first>
    - /home/fernando/Documents/datathon-2026/tsconfig.json (current `exclude` array on line 33)
    - /home/fernando/Documents/datathon-2026/.planning/phases/01-foundation-amplify-backend-skeleton/01-RESEARCH.md §3.b ("Create the bare `amplify/` directory and files", lines 250-310) — gives canonical content for backend.ts, package.json, tsconfig.json + the root tsconfig exclude
    - /home/fernando/Documents/datathon-2026/.planning/phases/01-foundation-amplify-backend-skeleton/01-RESEARCH.md "Gotchas & Landmines" G-5 (`amplify/` MUST be ESM), G-6 (root tsconfig MUST exclude `amplify/**/*`), G-13 (empty `defineBackend({})` is the success state)
    - /home/fernando/Documents/datathon-2026/.planning/phases/01-foundation-amplify-backend-skeleton/01-PATTERNS.md sections for `amplify/backend.ts` (lines 25-49), `amplify/package.json` (lines 53-77), `amplify/tsconfig.json` (lines 79-129), `tsconfig.json` (MODIFY) (lines 367-385)
    - /home/fernando/Documents/datathon-2026/.planning/phases/01-foundation-amplify-backend-skeleton/01-CONTEXT.md decisions D-05, D-06
    - /home/fernando/Documents/datathon-2026/AGENTS.md (Next.js 16 has breaking changes — verify against `node_modules/next/dist/docs/`; relevant here because the root tsconfig exclude exists to keep Next from compiling backend code, per G-6)
  </read_first>
  <action>
Create the three new files under `amplify/` and modify the root `tsconfig.json`. Use 2-space indent, double quotes, trailing newline at EOF (matches root tsconfig and package.json conventions per PATTERNS §"JSON Formatting").

**File 1: `/home/fernando/Documents/datathon-2026/amplify/backend.ts`**

```typescript
import { defineBackend } from "@aws-amplify/backend";

/**
 * Phase 1: Bare backend skeleton — no resources yet.
 * Phase 2 will add auth (Cognito + email/password).
 * @see https://docs.amplify.aws/nextjs/build-a-backend/
 */
defineBackend({});
```

Rules:
- The import MUST use double quotes (`"@aws-amplify/backend"`) — PATTERNS §"Quote Style" verified across `app/layout.tsx`, `next.config.ts`, etc.
- The literal call MUST be `defineBackend({})` (with the empty object) per D-05/D-06.
- The JSDoc block is the ONE sanctioned multiline-comment in this phase per PATTERNS §"File Header / Comment Convention" — it documents the intentional emptiness so future contributors understand it's not a stub forgotten.
- Do NOT import or reference `auth`, `data`, `storage`, or `function` resources. Do NOT add a top-level `await` or any other side-effects.

**File 2: `/home/fernando/Documents/datathon-2026/amplify/package.json`**

```json
{
  "type": "module"
}
```

Rules:
- Exactly two lines plus a trailing newline. No `name`, `version`, `dependencies`, or `description`. RESEARCH §3.b verified that Amplify's own scaffolder writes literally this content (G-5).

**File 3: `/home/fernando/Documents/datathon-2026/amplify/tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "es2022",
    "module": "es2022",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "skipLibCheck": true,
    "paths": {
      "$amplify/*": ["../.amplify/generated/*"]
    }
  }
}
```

Rules:
- This is verbatim from RESEARCH §3.b (sourced from Amplify's `package_manager_controller_base.ts` `tsConfigTemplate` constant). Do NOT change `target`, `module`, or any compiler option — Amplify's tsx-based loader expects these exact values.
- The `$amplify/*` alias is intentionally distinct from the root `@/*` alias — backend code uses it for generated typedefs (none exist yet in Phase 1; they appear in Phase 2+).
- No `include` or `exclude` array — backend tsc walks the `amplify/` directory by default.

**File 4 (MODIFY): `/home/fernando/Documents/datathon-2026/tsconfig.json`**

Change the existing `exclude` array on the last line of `compilerOptions`-sibling block (currently line 33) from:

```json
"exclude": ["node_modules"]
```

to:

```json
"exclude": ["node_modules", "amplify/**/*"]
```

Rules:
- This is the ONLY change to the root tsconfig. Do NOT touch `compilerOptions`, `include`, or any other key.
- Without this change (G-6), Next's TS compilation tries to include backend code and fails because `aws-cdk-lib` and `$amplify/*` paths are not configured in the root tsconfig.
- Preserve 2-space indent, no trailing comma in the array.

After all four files exist, verify the toolchain:

```bash
# Backend TS type-checks (no AWS calls — pure local tsc against amplify/)
cd amplify && npx tsc --noEmit && cd ..

# Next.js build still works (root tsconfig now excludes amplify/)
bun run build

# Strict gates from plan 01 still green
bun run lint
bun run typecheck
bun run audit
```

All MUST exit 0. The backend tsc step is what proves the manual install actually works without ever touching AWS — it's the strongest signal short of `npx ampx sandbox` (plan 05).
  </action>
  <verify>
    <automated>test -f amplify/backend.ts &amp;&amp; grep -qF 'defineBackend({})' amplify/backend.ts &amp;&amp; grep -qF 'from "@aws-amplify/backend"' amplify/backend.ts &amp;&amp; ! test -d amplify/auth &amp;&amp; ! test -d amplify/data &amp;&amp; ! test -d amplify/storage &amp;&amp; node -e "const p=require('./amplify/package.json'); if(p.type!=='module')process.exit(1); if(p.name||p.dependencies)process.exit(2)" &amp;&amp; node -e "const t=require('./amplify/tsconfig.json'); const c=t.compilerOptions; if(c.target!=='es2022'||c.module!=='es2022'||!c.strict)process.exit(1); if(!c.paths||!c.paths['\$amplify/*'])process.exit(2)" &amp;&amp; node -e "const t=require('./tsconfig.json'); if(!t.exclude.includes('amplify/**/*'))process.exit(1); if(!t.exclude.includes('node_modules'))process.exit(2)" &amp;&amp; (cd amplify &amp;&amp; npx --no-install tsc --noEmit) &amp;&amp; bun run lint &amp;&amp; bun run typecheck &amp;&amp; bun run build &amp;&amp; bun run audit</automated>
  </verify>
  <acceptance_criteria>
    - File `amplify/backend.ts` exists.
    - `grep -qF 'from "@aws-amplify/backend"' amplify/backend.ts` matches (double quotes per PATTERNS §"Quote Style").
    - `grep -qF 'defineBackend({})' amplify/backend.ts` matches (literal empty-object call).
    - `amplify/backend.ts` does NOT import or reference `auth`, `data`, `storage`, or `function` (verified via `! grep -E 'auth|data|storage|function' amplify/backend.ts` — none of these substrings should appear in any meaningful position; the import line has none either).
    - File `amplify/package.json` exists.
    - `node -e "const p=require('./amplify/package.json'); if(p.type!=='module')process.exit(1)"` exits 0 (G-5 satisfied).
    - `amplify/package.json` does NOT have `name`, `version`, `dependencies`, or `devDependencies` keys (verified via the same node check or `grep`).
    - File `amplify/tsconfig.json` exists.
    - `amplify/tsconfig.json` `compilerOptions.target` is `"es2022"`, `module` is `"es2022"`, `strict` is `true`, and `paths` contains the key `"$amplify/*"` mapped to `["../.amplify/generated/*"]`.
    - Directory `amplify/auth/` does NOT exist (`test ! -d amplify/auth` — G-4).
    - Directory `amplify/data/` does NOT exist (`test ! -d amplify/data` — D-05).
    - Directory `amplify/storage/` does NOT exist (`test ! -d amplify/storage` — D-05).
    - Root `tsconfig.json` `exclude` array contains BOTH `"node_modules"` AND `"amplify/**/*"` (G-6).
    - Root `tsconfig.json` `compilerOptions` is otherwise unchanged from before this plan (`@/*` path alias still present, etc.).
    - `cd amplify && npx --no-install tsc --noEmit` exits 0 (backend code type-checks with the new tsconfig + installed devDeps).
    - `bun run lint` exits 0.
    - `bun run typecheck` exits 0 (root tsc now skips `amplify/`, no module-resolution errors on `aws-cdk-lib`).
    - `bun run build` exits 0 (Next.js compiles successfully).
    - `bun run audit` exits 0.
  </acceptance_criteria>
  <done>Three new `amplify/` files exist matching the canonical Amplify Gen 2 manual-install templates; root `tsconfig.json` excludes `amplify/**/*`; backend TS type-checks; root build/lint/typecheck/audit all remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo source → CDK synthesis (build time) | `amplify/backend.ts` is the single declarative entry point that `ampx` reads to produce a CloudFormation template; an unintended import here would deploy unintended AWS resources. |
| Root TypeScript compile → backend TypeScript compile | Two separate tsconfig contexts; Next must NOT try to compile backend code (different module resolution, different path aliases). |
| `node_modules/` → registry | New devDeps (`@aws-amplify/backend`, `@aws-amplify/backend-cli`, `aws-cdk-lib`, etc.) pulled from the public npm registry; lockfile pins them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-09 | Elevation of Privilege | `amplify/backend.ts` accidentally imports an `auth` or `data` resource and deploys IAM/Cognito infra in Phase 1, breaking D-05/D-06 and prematurely exposing surfaces meant for Phase 2/3 | mitigate | Acceptance criteria explicitly assert `defineBackend({})` is empty AND that `amplify/auth`, `amplify/data`, `amplify/storage` directories do NOT exist. The literal `grep -qF 'defineBackend({})'` check is the structural guarantee. |
| T-1-10 | Tampering | Root tsconfig pulls backend code into the Next compile, mixing tier responsibilities and potentially exposing CDK API surface to client bundles | mitigate | Root tsconfig `exclude` adds `amplify/**/*` (G-6). `bun run build` is run in this plan as the smoke test that the exclusion works. |
| T-1-11 | Tampering | `amplify/package.json` missing `{"type": "module"}` causes `tsx` to load `backend.ts` as CommonJS, which silently breaks ESM-only imports from `@aws-amplify/backend` (G-5) | mitigate | Acceptance criterion asserts `amplify/package.json` `type === "module"` via a `node -e` script — runtime-equivalent to how `tsx` reads the field. |
| T-1-12 | Information Disclosure | Using `npm create amplify@latest` instead of manual install would write a `package-lock.json` (Bun-incompatible, G-3) and ship an unwanted `auth/` + `data/` template (G-4) | mitigate | Acceptance criterion: `package-lock.json` does NOT exist. The action explicitly forbids the scaffolder commands. |
| T-1-13 | Denial of Service (supply chain) | New Amplify transitives ship a fresh CVE that `bun audit` would catch | mitigate | `bun run audit` is run as a final gate in Task 1 and Task 2; if it surfaces a new advisory the plan halts before commit. |

Note: This plan installs no auth, no inputs, no networked endpoints inside the running app. The threat surface is structural (correct boundaries between tiers, correct lockfile pins, correct module system). The CDK synthesis path that runs in plan 05 is what would deploy real AWS resources — and Phase 1 deliberately keeps that synthesis output empty.
</threat_model>

<verification>
After both tasks complete, run from repo root:

```bash
# Files exist with expected content
test -f amplify/backend.ts && grep -qF 'defineBackend({})' amplify/backend.ts
test -f amplify/package.json && node -e "process.exit(require('./amplify/package.json').type==='module'?0:1)"
test -f amplify/tsconfig.json
node -e "process.exit(require('./tsconfig.json').exclude.includes('amplify/**/*')?0:1)"

# No unwanted directories
test ! -d amplify/auth && test ! -d amplify/data && test ! -d amplify/storage
test ! -f package-lock.json

# Backend TS valid (no AWS network calls)
cd amplify && npx --no-install tsc --noEmit && cd ..

# Root gates still green
bun run lint && bun run typecheck && bun run build && bun run audit
```
</verification>

<success_criteria>
- `amplify/backend.ts` exists with the literal call `defineBackend({})` (empty object) and imports `defineBackend` from `"@aws-amplify/backend"` using double quotes.
- `amplify/package.json` declares `{"type": "module"}` and has no other keys.
- `amplify/tsconfig.json` matches the canonical Amplify Gen 2 template (ES2022, strict, `$amplify/*` alias).
- Root `tsconfig.json` `exclude` includes both `"node_modules"` and `"amplify/**/*"`.
- `package.json` `devDependencies` contains `@aws-amplify/backend`, `@aws-amplify/backend-cli`, `aws-cdk-lib@2.244.0` (exact pin), `constructs`, `tsx`, `esbuild`.
- No `amplify/auth/`, `amplify/data/`, `amplify/storage/` directories exist (D-05/D-06 enforced).
- No `package-lock.json` exists at repo root (G-3 enforced).
- `cd amplify && npx --no-install tsc --noEmit` exits 0 (backend type-checks).
- `bun run lint`, `bun run typecheck`, `bun run build`, `bun run audit` all exit 0.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-amplify-backend-skeleton/01-03-SUMMARY.md` capturing:
- Final `package.json` `devDependencies` block (showing the six new entries with resolved versions)
- Final contents of `amplify/backend.ts`, `amplify/package.json`, `amplify/tsconfig.json`
- The diff applied to root `tsconfig.json` (`exclude` array change)
- Output of `cd amplify && npx --no-install tsc --noEmit` (must be empty + exit 0)
- Output of `bun run lint && bun run typecheck && bun run build && bun run audit` (all must be green)
- Confirmation that `package-lock.json` does NOT exist and that `amplify/{auth,data,storage}` were NOT created
</output>
