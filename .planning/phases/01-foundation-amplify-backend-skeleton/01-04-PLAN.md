---
phase: 01-foundation-amplify-backend-skeleton
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - README.md
autonomous: true
requirements:
  - INFRA-01
tags:
  - readme
  - docs
  - aws-setup
  - amplify
  - sandbox

must_haves:
  truths:
    - "README has a `## Setup` section placed BEFORE `## Getting Started`"
    - "Setup section walks the developer through: prerequisites (Node, Bun, AWS CLI v2), IAM user with `AmplifyBackendDeployFullAccess`, `aws configure --profile datathon-2026`, `aws sts get-caller-identity` validation, `cp .env.example .env.local`, `bun install`, and `AWS_PROFILE=datathon-2026 npx ampx sandbox`"
    - "Setup section sets the expectation that the FIRST `npx ampx sandbox` run takes 5-8 minutes (CDK bootstrap)"
    - "Setup section explains region precedence (env > profile > CLI default) and recommends `us-east-1`"
    - "Setup section documents tearing down the sandbox (`npx ampx sandbox delete`)"
    - "Setup section includes the AWS CLI v2 install command for Linux (the developer machine has no AWS CLI v2 installed)"
    - "Setup section uses `npx ampx`, NEVER `bunx ampx` (G-10)"
    - "Setup section documents BOTH access-key (`aws configure --profile`) and SSO (`aws configure sso`) flows"
    - "Existing `## Getting Started`, `## Learn More`, and `## Deploy on Vercel` sections are preserved"
  artifacts:
    - path: README.md
      provides: "Onboarding documentation including AWS CLI install, IAM setup, AWS profile config, sandbox lifecycle"
      contains: "## Setup"
  key_links:
    - from: README.md `## Setup`
      to: ".env.example"
      via: "Step 4 instructs `cp .env.example .env.local`"
      pattern: "cp \\.env\\.example \\.env\\.local"
    - from: README.md `## Setup`
      to: "amplify/backend.ts"
      via: "Step 6 runs `npx ampx sandbox` which reads the backend file created in plan 03"
      pattern: "npx ampx sandbox"
    - from: README.md `## Setup`
      to: "AWS IAM"
      via: "Step 1 attaches `AmplifyBackendDeployFullAccess` managed policy"
      pattern: "AmplifyBackendDeployFullAccess"
---

<objective>
Add a `## Setup` section to README.md that walks a fresh-clone developer through everything they need before plan 05 can run `npx ampx sandbox` successfully (D-10, D-11). This is Wave D from RESEARCH §"Recommended task wave ordering": no AWS network calls, no code changes — pure documentation. The section is the precondition for plan 05's manual deployment gate.

Purpose: The developer machine has no AWS CLI v2 installed (verified in RESEARCH §"Environment Availability") and `npx ampx sandbox` won't work without IAM credentials. The README is the contract that converts the in-repo Amplify skeleton (plan 03) into a deployable artifact (plan 05). Without this plan, plan 05 has no documented "where do I get an AWS account" answer.

Output: One file modified (`README.md`) with a new `## Setup` section inserted before `## Getting Started`. No code, no scripts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-amplify-backend-skeleton/01-CONTEXT.md
@.planning/phases/01-foundation-amplify-backend-skeleton/01-RESEARCH.md
@.planning/phases/01-foundation-amplify-backend-skeleton/01-VALIDATION.md
@.planning/phases/01-foundation-amplify-backend-skeleton/01-PATTERNS.md
@.planning/phases/01-foundation-amplify-backend-skeleton/01-03-SUMMARY.md
@README.md
@.env.example

<interfaces>
<!-- Existing README.md structure (from previous Read of the file):
  Line 1: intro paragraph
  Line 3: ## Getting Started
  Line 23: ## Learn More
  Line 32: ## Deploy on Vercel

  After this plan:
  Line 1: intro paragraph
  Line 3: ## Setup        ← NEW
    ### Prerequisites
    ### 1. Install AWS CLI v2 (if not already installed)
    ### 2. Create / pick an AWS IAM user
    ### 3. Configure your local AWS profile
    ### 4. Verify AWS access
    ### 5. Set the project env
    ### 6. Install dependencies
    ### 7. Deploy the sandbox
  ## Getting Started     ← preserved
  ## Learn More          ← preserved
  ## Deploy on Vercel    ← preserved (will be replaced in Phase 5; harmless for now)
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Insert `## Setup` section into README.md before `## Getting Started`</name>
  <files>README.md</files>
  <read_first>
    - /home/fernando/Documents/datathon-2026/README.md (current 36 lines — note `## Getting Started` on line 3 is the insertion point)
    - /home/fernando/Documents/datathon-2026/.env.example (created in plan 02 — README §5 references it)
    - /home/fernando/Documents/datathon-2026/.planning/phases/01-foundation-amplify-backend-skeleton/01-RESEARCH.md §8 ("README 'Setup' section", lines 526-601) — gives the canonical content (Prerequisites + 6 numbered steps + region paragraph)
    - /home/fernando/Documents/datathon-2026/.planning/phases/01-foundation-amplify-backend-skeleton/01-RESEARCH.md "Environment Availability" (lines 658-680) — confirms AWS CLI v2 is NOT installed locally and gives the Linux install command
    - /home/fernando/Documents/datathon-2026/.planning/phases/01-foundation-amplify-backend-skeleton/01-RESEARCH.md "Gotchas & Landmines" G-9 (CDK bootstrap takes 5-8 min on first run), G-10 (use `npx ampx`, NOT `bunx ampx`), G-12 (region precedence), and "Open Questions" #3 (document both SSO and access-key flows)
    - /home/fernando/Documents/datathon-2026/.planning/phases/01-foundation-amplify-backend-skeleton/01-CONTEXT.md decisions D-10, D-11, D-12, D-13
    - /home/fernando/Documents/datathon-2026/.planning/phases/01-foundation-amplify-backend-skeleton/01-PATTERNS.md "README.md (MODIFY ...)" section (lines 418-444) — confirms the structural placement and existing heading style (`##` for top-level)
  </read_first>
  <action>
Modify `/home/fernando/Documents/datathon-2026/README.md` by inserting a new `## Setup` section between the existing intro paragraph (lines 1-2) and the existing `## Getting Started` heading (currently line 3). Preserve every existing line; do NOT modify or delete `## Getting Started`, `## Learn More`, or `## Deploy on Vercel` sections.

The new `## Setup` section content (insert verbatim, with the leading blank line for spacing):

```markdown

## Setup

This project ships with a minimal AWS Amplify Gen 2 backend skeleton (`amplify/backend.ts` — currently a bare `defineBackend({})`). Before you can deploy a personal sandbox, configure your local AWS environment.

### Prerequisites

- Node.js ≥ 20.6.0 — verify with `node --version`
- Bun ≥ 1.3 — verify with `bun --version` (project uses Bun for `bun audit` and as the package manager)
- AWS CLI v2 — verify with `aws --version` (install instructions in step 1 below if missing)
- An AWS account and an IAM user (or IAM Identity Center user) with the `AmplifyBackendDeployFullAccess` managed policy attached.

### 1. Install AWS CLI v2 (if not already installed)

On Linux x86_64:

```bash
curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
unzip awscliv2.zip
sudo ./aws/install -i /usr/local/aws-cli -b /usr/local/bin
aws --version
```

For macOS, Windows, or other architectures see https://aws.amazon.com/cli/.

### 2. Create / pick an AWS IAM user

In the AWS Console → IAM → Users → "Create user".
Attach managed policy: `AmplifyBackendDeployFullAccess`.
Generate an access key (Other → CLI). Save the access key ID and secret somewhere safe — you'll paste them into `aws configure` in the next step.

### 3. Configure your local AWS profile

Pick ONE of the two flows:

**Option A — Long-lived access keys (simplest):**

```bash
aws configure --profile datathon-2026
# AWS Access Key ID:     <paste from step 2>
# AWS Secret Access Key: <paste from step 2>
# Default region name:   us-east-1
# Default output format: json
```

**Option B — IAM Identity Center / SSO (AWS-recommended for daily use):**

```bash
aws configure sso
# Follow the interactive prompts. Use the same profile name (datathon-2026)
# so the rest of this README applies unchanged.
```

Both flows work with `npx ampx sandbox`.

> **Why no access keys in `.env`?** AWS access keys belong in `~/.aws/credentials` (managed by `aws configure`), NOT in `.env.local`. See the warning block in `.env.example`.

### 4. Verify AWS access

```bash
aws sts get-caller-identity --profile datathon-2026
# Should print { "UserId": "...", "Account": "...", "Arn": "..." } with exit code 0.
```

If this fails, fix it before continuing — `npx ampx sandbox` won't work either.

### 5. Set the project env

```bash
cp .env.example .env.local
# Edit .env.local if your profile name differs from `datathon-2026`.
```

`.env.local` is gitignored. The repo only ever tracks `.env.example`.

### 6. Install dependencies

```bash
bun install
```

### 7. Deploy the sandbox

```bash
AWS_PROFILE=datathon-2026 npx ampx sandbox
```

Use `npx ampx` (NOT `bunx ampx`) — Amplify's CLI tooling currently has rough edges with Bun's binary launcher.

**First run takes ~5-8 minutes** because it provisions the CDK bootstrap stack (`CDKToolkit`) in your chosen region. Subsequent runs in the same region take ~30 seconds (CDK hot-swap deploys).

The command stays in watch mode (any save to a file under `amplify/` triggers a hot redeploy). Press `Ctrl+C` to exit watch mode without destroying the stack.

When you're done for the day and want to fully tear down:

```bash
AWS_PROFILE=datathon-2026 npx ampx sandbox delete
```

> **Region precedence (highest wins):**
> 1. `AWS_REGION` env var (set in `.env.local`)
> 2. The region recorded in `~/.aws/config` for the AWS_PROFILE above
> 3. AWS CLI default region
>
> `us-east-1` is recommended (broadest service availability, low latency from US/LATAM). Change `AWS_REGION` in `.env.local` to deploy elsewhere.

```

Hard rules:
- Insert position: AFTER the existing line 1-2 intro paragraph, BEFORE the existing `## Getting Started` heading (currently line 3).
- The first character of the inserted block is the leading blank line (a single `\n`) so the new `## Setup` heading is separated from the intro paragraph by exactly one blank line. The block ends with a trailing blank line so `## Getting Started` is also visually separated.
- Use `npx ampx` in EVERY command example. Never use `bunx ampx` (G-10) or `bun ampx`. Never use the legacy Gen 1 `amplify init` / `amplify push` (also confusable per RESEARCH §"State of the Art"; do not include them anywhere).
- Use the literal profile name `datathon-2026` consistently throughout the section. CONTEXT D-10 marks this as a suggestion under Claude's discretion — once chosen, use it consistently so steps 3-7 wire together.
- The fenced code blocks above contain triple backticks INSIDE a markdown content block — when inserting, ensure the outer markdown structure is preserved (the example shows what the README will literally contain, so the triple backticks ARE part of the README content).
- Do NOT add any other top-level sections, do NOT add a Table of Contents, do NOT replace the existing `## Deploy on Vercel` (that's Phase 5's job, not this one — it's harmless for Phase 1).
- Do NOT modify `app/`, `package.json`, `.env.example`, or any other file. README is the only file this plan touches.

After saving, verify:

```bash
# Section exists and is positioned correctly
grep -n '^## Setup' README.md      # MUST appear BEFORE
grep -n '^## Getting Started' README.md  # this line

# All required content is present
grep -qF 'AmplifyBackendDeployFullAccess' README.md
grep -qF 'aws configure --profile datathon-2026' README.md
grep -qF 'aws sts get-caller-identity --profile datathon-2026' README.md
grep -qF 'cp .env.example .env.local' README.md
grep -qF 'AWS_PROFILE=datathon-2026 npx ampx sandbox' README.md
grep -qF 'AWS_PROFILE=datathon-2026 npx ampx sandbox delete' README.md
grep -qF '5-8 min' README.md
grep -qF 'us-east-1' README.md
grep -qiE 'aws configure sso' README.md      # both flows documented
grep -qF 'awscli-exe-linux-x86_64.zip' README.md  # AWS CLI install step

# Anti-patterns absent
! grep -qF 'bunx ampx' README.md     # G-10
! grep -qF 'amplify init' README.md  # Gen 1 confusion
! grep -qF 'amplify push' README.md  # Gen 1 confusion

# Existing sections preserved
grep -qF '## Getting Started' README.md
grep -qF '## Learn More' README.md
grep -qF '## Deploy on Vercel' README.md

# Repo gates still green (README change shouldn't affect anything)
bun run lint && bun run typecheck && bun run build && bun run audit
```

All checks MUST pass.
  </action>
  <verify>
    <automated>SETUP_LINE=$(grep -n '^## Setup' README.md | head -1 | cut -d: -f1) &amp;&amp; GS_LINE=$(grep -n '^## Getting Started' README.md | head -1 | cut -d: -f1) &amp;&amp; [ -n "$SETUP_LINE" ] &amp;&amp; [ -n "$GS_LINE" ] &amp;&amp; [ "$SETUP_LINE" -lt "$GS_LINE" ] &amp;&amp; grep -qF 'AmplifyBackendDeployFullAccess' README.md &amp;&amp; grep -qF 'aws configure --profile datathon-2026' README.md &amp;&amp; grep -qF 'aws sts get-caller-identity --profile datathon-2026' README.md &amp;&amp; grep -qF 'cp .env.example .env.local' README.md &amp;&amp; grep -qF 'AWS_PROFILE=datathon-2026 npx ampx sandbox' README.md &amp;&amp; grep -qF 'AWS_PROFILE=datathon-2026 npx ampx sandbox delete' README.md &amp;&amp; grep -qF '5-8 min' README.md &amp;&amp; grep -qF 'us-east-1' README.md &amp;&amp; grep -qiE 'aws configure sso' README.md &amp;&amp; grep -qF 'awscli-exe-linux-x86_64.zip' README.md &amp;&amp; ! grep -qF 'bunx ampx' README.md &amp;&amp; ! grep -qF 'amplify init' README.md &amp;&amp; ! grep -qF 'amplify push' README.md &amp;&amp; grep -qF '## Getting Started' README.md &amp;&amp; grep -qF '## Learn More' README.md &amp;&amp; grep -qF '## Deploy on Vercel' README.md &amp;&amp; bun run lint &amp;&amp; bun run typecheck &amp;&amp; bun run build &amp;&amp; bun run audit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '^## Setup' README.md` returns a line number SMALLER than `grep -n '^## Getting Started' README.md`'s line number (Setup precedes Getting Started).
    - `grep -qF 'AmplifyBackendDeployFullAccess' README.md` matches (D-10 step 1).
    - `grep -qF 'aws configure --profile datathon-2026' README.md` matches (D-10 step 4).
    - `grep -qF 'aws sts get-caller-identity --profile datathon-2026' README.md` matches (D-10 step 5).
    - `grep -qF 'cp .env.example .env.local' README.md` matches (links Setup to plan 02's `.env.example`).
    - `grep -qF 'AWS_PROFILE=datathon-2026 npx ampx sandbox' README.md` matches (D-10 step 6 — `npx`, NOT `bunx`, per G-10).
    - `grep -qF 'AWS_PROFILE=datathon-2026 npx ampx sandbox delete' README.md` matches (cleanup; D-10 step 6).
    - `grep -qF '5-8 min' README.md` matches (CDK bootstrap expectation; G-9).
    - `grep -qF 'us-east-1' README.md` matches (recommended region; D-11).
    - `grep -qiE 'aws configure sso' README.md` matches (SSO flow documented; A5/Open Question #3).
    - `grep -qF 'awscli-exe-linux-x86_64.zip' README.md` matches (AWS CLI v2 install command, since the local machine doesn't have it).
    - `! grep -qF 'bunx ampx' README.md` (G-10 — never recommend `bunx ampx`).
    - `! grep -qF 'amplify init' README.md` (no Gen 1 confusion).
    - `! grep -qF 'amplify push' README.md` (no Gen 1 confusion).
    - `! grep -qE 'AKIA[A-Z0-9]{16}' README.md` (no real or example access key IDs leaked into docs).
    - `grep -qF '## Getting Started' README.md` matches (existing section preserved).
    - `grep -qF '## Learn More' README.md` matches (existing section preserved).
    - `grep -qF '## Deploy on Vercel' README.md` matches (existing section preserved; Phase 5 will replace).
    - `bun run lint`, `bun run typecheck`, `bun run build`, `bun run audit` all exit 0 (README change has no code impact; these are sanity checks).
  </acceptance_criteria>
  <done>README has a `## Setup` section before `## Getting Started` covering AWS CLI install, IAM setup, profile config, env file copy, dependencies, and sandbox lifecycle (with both deploy and teardown commands and the 5-8 min CDK bootstrap warning). All anti-patterns (bunx ampx, Gen 1 commands, leaked access keys) are absent. The existing README sections are intact and the repo gates are still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → developer | Documentation is the operational contract; misleading instructions can cause secrets to land in tracked files or wrong AWS accounts to be billed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-14 | Information Disclosure | Documentation example contains a real or realistic-looking AWS access key (`AKIA…`) that someone copy-pastes verbatim | mitigate | Acceptance criterion explicitly forbids any string matching `AKIA[A-Z0-9]{16}` in README.md; the example flow shows `<paste from step 2>` placeholders only. |
| T-1-15 | Information Disclosure | Developer follows ambiguous instructions and writes AWS keys into `.env.local` | mitigate | README §3 prominently states "AWS access keys belong in `~/.aws/credentials`, NOT in `.env.local`" and points to the warning in `.env.example` (which itself contains a multi-line warning per plan 02). |
| T-1-16 | Spoofing / Authorization Failure | Developer uses an over-privileged IAM user (e.g., AdministratorAccess) instead of the scoped policy, expanding blast radius if creds leak | mitigate | README §2 names exactly `AmplifyBackendDeployFullAccess` — the AWS-managed policy scoped to Amplify's CDK needs. Documented as the recommended attachment. |
| T-1-17 | Operational confusion (Repudiation) | Developer assumes the first sandbox run is broken because it takes 5-8 minutes, manually kills it, leaves a partial CDK bootstrap stack | mitigate | README §7 explicitly sets the "first run takes ~5-8 minutes" expectation (G-9) so developers don't kill the process prematurely. |
| T-1-18 | Wrong-region drift (Tampering) | Developer's AWS profile is configured for one region but `.env.local` says another, leading to surprise charges in the wrong region | mitigate | README §7 includes the region-precedence callout (G-12) so developers know `AWS_REGION` env var wins over the profile's recorded region. |

Note: This plan adds documentation only — no code, no inputs, no networked endpoints. The threat surface is the documentation itself; the mitigations above are anchored in concrete README content the acceptance criteria verify.
</threat_model>

<verification>
After the task completes, run from repo root:

```bash
# Section ordering (Setup before Getting Started)
SETUP_LINE=$(grep -n '^## Setup' README.md | head -1 | cut -d: -f1)
GS_LINE=$(grep -n '^## Getting Started' README.md | head -1 | cut -d: -f1)
echo "Setup at line $SETUP_LINE; Getting Started at line $GS_LINE"
test "$SETUP_LINE" -lt "$GS_LINE"

# Required strings present (full list in acceptance criteria)
grep -qF 'AmplifyBackendDeployFullAccess' README.md
grep -qF 'AWS_PROFILE=datathon-2026 npx ampx sandbox' README.md
grep -qF '5-8 min' README.md

# Anti-patterns absent
! grep -qF 'bunx ampx' README.md
! grep -qE 'AKIA[A-Z0-9]{16}' README.md

# Repo gates
bun run lint && bun run typecheck && bun run build && bun run audit
```
</verification>

<success_criteria>
- README has a `## Setup` section located between the intro paragraph and `## Getting Started`.
- The Setup section covers: prerequisites, AWS CLI install (Linux), IAM user + `AmplifyBackendDeployFullAccess`, both `aws configure --profile` and `aws configure sso` flows, `aws sts get-caller-identity` validation, env file copy, `bun install`, `npx ampx sandbox` (with the 5-8 min warning), region precedence, and `npx ampx sandbox delete`.
- Every command uses `npx ampx`, never `bunx ampx`. No Gen 1 commands (`amplify init`, `amplify push`) appear.
- No AWS access key strings leaked into docs.
- The existing `## Getting Started`, `## Learn More`, `## Deploy on Vercel` sections are intact.
- `bun run lint`, `bun run typecheck`, `bun run build`, `bun run audit` all exit 0.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-amplify-backend-skeleton/01-04-SUMMARY.md` capturing:
- The line numbers where `## Setup` and `## Getting Started` now sit
- The section headings that were added (the seven `### N. ...` subheadings)
- Confirmation that no anti-patterns (`bunx ampx`, `amplify init`, `amplify push`, `AKIA…` keys) appear
- Confirmation that the existing README sections are preserved
- Output of `bun run lint && bun run typecheck && bun run build && bun run audit` (must all be green)
</output>
